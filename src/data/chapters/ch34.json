{
  "id": 34,
  "slug": "java",
  "stage": 4,
  "title": "Java + Spring Boot",
  "subtitle": "企業 / 銀行",
  "icon": "coffee",
  "description": "Java + Spring Boot：企業 / 銀行",
  "status": "published",
  "difficulty": "intermediate",
  "prerequisites": [
    7
  ],
  "estimatedHours": 16,
  "outcomes": [
    "理解核心概念",
    "實作主流工具",
    "整章實戰可發佈作品",
    "知道生態 / 接案行情",
    "對齊 2026 業界主流"
  ],
  "lessons": [
    {
      "title": "Java 為什麼仍是主流",
      "oneLineSummary": "2026 Java 仍是企業 backend 第一",
      "analogy": "銀行、政府、大公司全 Java。",
      "content": "## Java 為什麼仍是主流\n\n### JVM：30 年的穩定基石\n\nJava 最強的超能力叫做「JVM（Java Virtual Machine —— Java 虛擬機）」。簡單講，JVM 就像一個萬能翻譯機——無論你在 Windows、Mac、Linux，JVM 都能把 Java 程式碼「翻譯」成那個系統聽得懂的指令。正因為有這層保護膜，Java 程式可以「一次寫、到處跑」。\n\n這個技術已經穩定運行了 30 多年。**穩定有多重要？**在銀行每天轉帳幾百萬筆、保險公司要處理數十萬個理賠案件、電信公司要服務千萬用戶的世界裡，「不出錯」值幾千萬。Java 和 JVM 就是那種能在半夜 3 點讓系統管理員睡著覺的技術——**可靠到不需要擔心**。\n\n### 生態完整到令人發指\n\n想像你要蓋一棟 101 大樓。Java 的世界就像——所有的磚頭、鋼筋、電線、玻璃、電梯、空調都已經有廠商做好了，你只要選一個牌子、裝上去就行。\n\n這套完整的「零件庫」叫 **Spring 生態**。Spring 包括：\n- **Spring Boot**：5 分鐘快速建專案的工具\n- **Spring Data**：資料庫操作變得像說話一樣簡單\n- **Spring Security**：帳密、權限、登入全自動處理\n- **Spring Cloud**：大型微服務架構的標準套組\n\n另外還有 Maven、Gradle（專案管理工具）、JUnit、Mockito（測試工具）、Docker 整合……這些東西在 Java 世界已經磨合 20 年，互相搭配得天衣無縫。**換句話說：你想做的事，十之八九已經有現成輪子可用。**\n\n### 招募市場最龐大\n\n查閱台灣主流人力銀行（1111、104）：Java / Spring Boot 職缺數最常排在前三名。為什麼？因為大企業多。\n\n**「大企業多」= 「選擇多」= 「職涯穩定」**\n\n你學 Java 可以選：\n- 銀行前三名（台灣銀、國泰、第一金）\n- 保險龍頭（國泰人壽、南山、新光）\n- 電信三雄（台灣大、遠傳、中華）\n- 電商巨頭（蝦皮、PChome、Momo）\n- 金科新貴（街口支付、歐付寶）\n\n這些公司年年招，人才流動量大（升遷、離職），所以 Java 工程師的職缺幾乎永遠缺人。\n\n### 薪水為什麼高？（不只是行情）\n\n台灣 Java 工程師行情：\n- 新鮮人：NT$ 50-70k\n- 2-3 年經驗：NT$ 70-100k\n- 5 年以上：NT$ 100-150k\n- Senior / Tech Lead：NT$ 150-180k+\n\n**為什麼比 Node / Python 工程師高？**不全是因為「Java 更難」，而是：\n\n1. **企業體質**——銀行、保險這些公司是「舊產業」，獲利穩定、錢多，給工資時下手大方\n2. **難度成本**——大型系統（幾十萬行程式碼）需要經驗豐富的人，新鮮人進不來，所以在職者水漲船高\n3. **責任重**——你的程式碼可能掌管百億資金，出錯要賠的不只是薪水\n\n簡言之：**高薪不是天上掉下來，而是企業賺錢能力強 + 工作責任重的結果**。\n\n---\n\n## Java 的演進：從恐龍變聰明\n\n### Java 8（2014）—— 現代 Java 的起點\n\nJava 8 是個轉折點。在它之前，Java 很「冗長」——同樣邏輯要寫超多行。Java 8 引入兩個黑科技：\n\n**Lambda —— 匿名函式**\n\n```java\n// Java 8 之前（悲）\nList<Integer> numbers = Arrays.asList(1, 2, 3, 4, 5);\nCollections.sort(numbers, new Comparator<Integer>() {\n    @Override\n    public int compare(Integer a, Integer b) {\n        return a.compareTo(b);\n    }\n});\n\n// Java 8 之後（爽）\nList<Integer> numbers = Arrays.asList(1, 2, 3, 4, 5);\nnumbers.sort((a, b) -> a.compareTo(b));\n```\n\n**Stream —— 流式處理**\n\n```java\n// 過濾、轉換、計算都變得像讀英文一樣自然\nList<Integer> result = numbers.stream()\n    .filter(n -> n > 2)           // 只要大於 2 的\n    .map(n -> n * 2)              // 每個都乘以 2\n    .collect(Collectors.toList()); // 收集成 List\n```\n\nJava 8 之後，Java 程式碼開始「可讀」了。這版本至今仍有很多公司在用。\n\n### Java 17（2021）—— LTS 企業穩定版\n\n**LTS = Long-Term Support（長期支援版）**——Oracle 承諾免費更新 5 年、付費支援到 10 年。企業很愛選 LTS 版本，因為「不用擔心 3 年後官方突然放棄」。\n\nJava 17 新增：\n\n**Records —— 簡化 Data Class**\n\n```java\n// 之前要寫一堆 getter / setter / equals / hashCode\npublic class User {\n    private String name;\n    private int age;\n    \n    public User(String name, int age) { ... }\n    public String getName() { ... }\n    public int getAge() { ... }\n    @Override\n    public boolean equals(Object o) { ... }\n    // 還要寫更多...\n}\n\n// Java 17 用 record 一行搞定\npublic record User(String name, int age) {}\n```\n\n**Pattern Matching —— 更聰明的 if-else**\n\n```java\n// 之前\nObject obj = \"hello\";\nif (obj instanceof String) {\n    String str = (String) obj;\n    System.out.println(str.length());\n}\n\n// Java 17\nif (obj instanceof String str) {\n    System.out.println(str.length());\n}\n```\n\n### Java 21（2023）—— Virtual Threads 革命\n\n**Virtual Threads（虛擬執行緒）**是最近 Java 圈最興奮的功能。簡白講：\n\n傳統 Java 建立一個執行緒很「貴」——每個執行緒消耗約 1MB 記憶體。如果要同時處理 100 萬個網路連線，就得建 100 萬個執行緒，記憶體一下炸裂。\n\nVirtual Threads 不一樣——它是「假執行緒」，建立它幾乎不消耗記憶體。同一套程式碼，可以輕鬆處理 **百萬級連線**：\n\n```java\n// 傳統方法：每個連線用一個執行緒（記憶體爆炸）\nfor (int i = 0; i < 1_000_000; i++) {\n    new Thread(() -> handleConnection()).start();\n}\n\n// Java 21 Virtual Threads：記憶體友善\nfor (int i = 0; i < 1_000_000; i++) {\n    Thread.startVirtualThread(() -> handleConnection());\n}\n```\n\n**真實案例**：某大型網路服務公司升級到 Java 21 後，同樣的硬體，吞吐量增加 10 倍——不改一行業務邏輯，只改 JVM。\n\nJava 25 預計也是 LTS 版本，維持「每 2 年一個 LTS」的節奏。\n\n---\n\n## Spring Boot vs Spring：一場無痛升級\n\n### Spring（古董時代）\n\n「Spring」原本是一個通用框架，提供「相依性注入（DI）」這個概念——讓程式碼更好測試、更好維護。\n\n但初期 Spring 有個地獄：**XML 設定檔地獄**\n\n```xml\n<!-- applicationContext.xml -->\n<beans xmlns=\"...\">\n    <bean id=\"userService\" class=\"com.example.UserService\">\n        <constructor-arg ref=\"userRepository\" />\n    </bean>\n    <bean id=\"userRepository\" class=\"com.example.UserRepository\">\n        <property name=\"dataSource\" ref=\"dataSource\" />\n    </bean>\n    <bean id=\"dataSource\" class=\"org.springframework.jdbc.datasource.DriverManagerDataSource\">\n        <property name=\"driverClassName\" value=\"com.mysql.cj.jdbc.Driver\" />\n        <property name=\"url\" value=\"jdbc:mysql://localhost:3306/mydb\" />\n        <!-- 還要寫超多... -->\n    </bean>\n</beans>\n```\n\n一個簡單的專案要寫幾百行 XML，維護死人。\n\n### Spring Boot（救世主）\n\nSpring Boot 解決了這個痛點——**零 XML、自動配置**：\n\n```java\n// Application.java\n@SpringBootApplication\npublic class Application {\n    public static void main(String[] args) {\n        SpringApplication.run(Application.class, args);\n    }\n}\n\n// application.properties\nspring.datasource.url=jdbc:mysql://localhost:3306/mydb\nspring.datasource.username=root\nspring.datasource.password=***\n```\n\n**5 分鐘建好一個完整後端專案**：\n\n🖥️ 指令行\n```bash\n# 用 Spring Boot 官方工具快速建專案\ncurl https://start.spring.io/starter.zip \\\n  -d dependencies=web,mysql,jpa \\\n  -d language=java \\\n  -o myproject.zip\nunzip myproject.zip\ncd myproject\nmvn spring-boot:run\n```\n\n💬 輸出\n```\nStarted Application in 3.142 seconds\n```\n\n然後你就有了一個可運行的 REST API 框架、資料庫連線、錯誤處理全都自動裝好。**Spring Boot = Spring + 開箱即用**。\n\n---\n\n## 適合 vs 不適合：誠實的評估\n\n### ✅ 適合 Java\n\n- **企業 / 銀行 / 保險 / 政府後端**：要穩、要能長期維護、要好招人，Java 是預設答案。\n- **大型、長生命週期的系統**：幾十萬行程式碼、要跑十幾年，Java 的強型別與成熟工具鏈讓大團隊接手不痛苦。\n- **高並發後端**：Java 21 的 Virtual Threads 讓它能穩穩扛住百萬級連線。\n- **想進大公司當工程師**：職缺多、薪水穩，履歷上有 Java + Spring 很吃香。\n\n### ❌ 不適合 Java\n\n- **一個人快速做 side project / MVP**：Java 樣板多、啟動慢，同樣一晚上，用 Python 或 Node 你可能已經上線了。\n- **Serverless / 極短命的函式**：冷啟動要好幾秒（除非用 GraalVM Native Image），不如 Go / Node 靈巧。\n- **小型腳本、爬蟲、一次性資料處理**：殺雞用牛刀，Python 更順手。\n\n一句話：**Java 適合「大、久、要穩」的系統，不適合「小、快、拋棄式」的東西。**\n\n## 跟 Node / Go 比\n\n| 面向 | Java | Node.js | Go |\n|---|---|---|---|\n| **啟動速度** | 慢（JVM 要暖機） | 快 | 最快 |\n| **記憶體用量** | 高 | 中 | 低 |\n| **生態成熟度** | 最完整（20+ 年） | 巨大（npm） | 中、成長中 |\n| **企業 / 金融採用** | 最多 | 中 | 中（雲原生多） |\n| **開發速度** | 慢（樣板多） | 快 | 中 |\n| **台灣職缺** | 多、穩定 | 多、成長 | 少但薪水高 |\n\n**Java 最大的優勢不是「跑最快」或「最新」，而是「生態完整、企業採用最廣、招募市場最大、薪水穩」。** Node 開發快、Go 輕巧省資源，但要進銀行、保險、政府這種「大而穩」的系統，Java 仍是第一選擇。\n\n## Indie 接案\n\n老實說，**Java 不是 Indie 開發者接案的首選**。原因很直接：\n\n- 起步慢、樣板多，一個人要在短時間內做出 MVP，Java 的節奏偏重。\n- 便宜的共享主機不太跑 Java，部署成本比 PHP / Node 高一點。\n- 個人接案的小網站、小工具，客戶更常指定 WordPress（PHP）或 Node。\n\n**那什麼時候 Java 對個人有價值？** 當你的目標是「進企業當工程師」——這時 Java + Spring 的履歷含金量很高。想走純 Indie / 自由接案，同樣的時間拿去學 Node 或 PHP 通常更划算；想進大公司、金融業，Java 是敲門磚。（這是方向建議，不是收入保證——實際接不接得到案、薪水多少，還是看作品、經驗與面試表現。）\n\n## 心法\n\n1. **Java 不時髦，但「無聊得可靠」正是它的價值**——大系統要的是穩、可預測、找得到人維護。\n2. **選 Java = 選「進企業 / 金融 / 政府」這條路**，不是選「一個人快速 ship」。\n3. **現代 Java（17 / 21）已經不冗長了**——Records、Pattern Matching、Virtual Threads 讓它又穩又不難寫。\n4. **學 Java 幾乎等於學 Spring Boot**——這是企業後端的實質標準。\n\n**☕ 用人話講**\n\nJava 為什麼還是主流？因為它「大、久、穩」。**一句話記住**：Java 就像高鐵——不是最酷的交通工具，但要載很多人、天天準點、出事機率極低時，大家還是選它。想進銀行 / 大企業當後端工程師，Java + Spring 是最穩的門票；想一個人快速做小東西，才換更輕的語言。",
      "tip": {
        "type": "practical",
        "text": "Java 不時髦、但養老穩定、薪水高。"
      },
      "id": "34.1",
      "number": "LESSON 34.1",
      "xp": 10,
      "playgrounds": [
        {
          "key": "34.1.auto",
          "language": "java",
          "title": "第一支 Java 程式",
          "initialCode": "public class Main {\n    public static void main(String[] args) {\n        // Java：企業後端的常青樹\n        System.out.println(\"哈囉，我在學 Java！\");\n\n        String[] langs = {\"Java\", \"Kotlin\"};\n        for (String lang : langs) {\n            System.out.println(\"JVM 語言：\" + lang);\n        }\n    }\n}",
          "hint": "Java：企業後端的常青樹。"
        }
      ],
      "exercise": {
        "question": "Java 誕生近 30 年為什麼仍是企業後端主流(JVM 成熟穩定、生態龐大、強型別、跨平台、人才多)？『JVM 一次編譯到處執行』的真正價值在企業環境是什麼？相比新語言,Java 的『無聊但可靠』對大型長期系統為什麼是優點？",
        "hint": "JVM 成熟、生態大、人才多;穩定可靠勝過潮。",
        "answer": "Java 主流因:JVM 極成熟穩定(調校/監控工具完備)、生態龐大(Spring 等企業框架、無數函式庫)、強型別利於大團隊維護、跨平台(一次編譯到處跑)、人才市場大好招募。企業價值:同一份 bytecode 跑在各種伺服器/OS、有成熟的效能調校與可觀測性。『無聊但可靠』對大型長期系統是優點:系統要跑十幾年、要能找到人維護、要有踩過坑的成熟方案——穩定、可預測、生態厚的技術長期成本低,勝過新潮但未知的選擇。"
      },
      "outline": [
        {
          "level": 2,
          "text": "為什麼仍主流"
        },
        {
          "level": 2,
          "text": "Java 演進"
        },
        {
          "level": 2,
          "text": "Spring Boot vs Spring"
        },
        {
          "level": 2,
          "text": "適合 vs 不適合"
        },
        {
          "level": 3,
          "text": "✅ 適合"
        },
        {
          "level": 3,
          "text": "❌ 不適合"
        },
        {
          "level": 2,
          "text": "跟 Node / Go 比"
        },
        {
          "level": 2,
          "text": "Indie 接案"
        },
        {
          "level": 2,
          "text": "心法"
        }
      ],
      "miniQuiz": {
        "question": "根據這堂課的內容，Java 相比 Node.js 和 Go 最大的優勢是什麼？",
        "options": [
          {
            "value": "a",
            "label": "啟動速度快、記憶體用量低"
          },
          {
            "value": "b",
            "label": "生態完整、企業職缺多、薪水高"
          },
          {
            "value": "c",
            "label": "開發速度快、適合 Indie 接案"
          },
          {
            "value": "d",
            "label": "新功能最多、技術最先進"
          }
        ],
        "answer": "b",
        "explanation": "課程強調 Java 仍是主流的核心原因是『生態龐大、企業採用廣、招募市場大、薪水高』，這些正是它相比 Node 和 Go 的競爭優勢。而 a、c、d 都不符合課程內容——Java 啟動慢、開發速度慢、不適合 Indie，也不是以「新」取勝。"
      }
    },
    {
      "title": "Spring Boot 起步 + REST API",
      "oneLineSummary": "5 分鐘建一個企業級 API",
      "analogy": "Java 後端標準框架。",
      "content": "## 起步\n```bash\n# Spring Initializr\ncurl https://start.spring.io/starter.zip \\\n  -d dependencies=web,data-jpa,h2 \\\n  -d type=gradle-project \\\n  -d language=java \\\n  -d javaVersion=21 \\\n  -o my-app.zip\n```\n\n或網頁版：start.spring.io。\n\n## Hello World\n```java\n@SpringBootApplication\n@RestController\npublic class App {\n    public static void main(String[] args) {\n        SpringApplication.run(App.class, args);\n    }\n    \n    @GetMapping(\"/\")\n    public String hello() {\n        return \"Hello, World!\";\n    }\n}\n```\n\n```bash\n./gradlew bootRun\n```\n\n## REST Controller\n```java\n@RestController\n@RequestMapping(\"/api/users\")\npublic class UserController {\n    \n    @Autowired\n    private UserService userService;\n    \n    @GetMapping\n    public List<User> list() {\n        return userService.findAll();\n    }\n    \n    @GetMapping(\"/{id}\")\n    public User get(@PathVariable Long id) {\n        return userService.findById(id)\n            .orElseThrow(() -> new ResponseStatusException(NOT_FOUND));\n    }\n    \n    @PostMapping\n    @ResponseStatus(CREATED)\n    public User create(@Valid @RequestBody CreateUserDto dto) {\n        return userService.create(dto);\n    }\n    \n    @PutMapping(\"/{id}\")\n    public User update(@PathVariable Long id, @Valid @RequestBody UpdateUserDto dto) {\n        return userService.update(id, dto);\n    }\n    \n    @DeleteMapping(\"/{id}\")\n    @ResponseStatus(NO_CONTENT)\n    public void delete(@PathVariable Long id) {\n        userService.delete(id);\n    }\n}\n```\n\n## JPA Entity\n```java\n@Entity\n@Table(name = \"users\")\npublic class User {\n    @Id\n    @GeneratedValue(strategy = IDENTITY)\n    private Long id;\n    \n    @Column(unique = true, nullable = false)\n    private String email;\n    \n    private String name;\n    \n    @CreatedDate\n    private LocalDateTime createdAt;\n    \n    // getters / setters\n}\n```\n\n## Repository\n```java\n@Repository\npublic interface UserRepository extends JpaRepository<User, Long> {\n    Optional<User> findByEmail(String email);\n    List<User> findByCreatedAtAfter(LocalDateTime date);\n}\n```\n\nJPA 自動生 SQL、不用寫 boilerplate。\n\n## Service\n```java\n@Service\npublic class UserService {\n    @Autowired\n    private UserRepository userRepository;\n    \n    public User create(CreateUserDto dto) {\n        if (userRepository.findByEmail(dto.getEmail()).isPresent()) {\n            throw new IllegalArgumentException(\"Email exists\");\n        }\n        \n        User user = new User();\n        user.setEmail(dto.getEmail());\n        user.setName(dto.getName());\n        return userRepository.save(user);\n    }\n}\n```\n\n## Validation\n```java\npublic class CreateUserDto {\n    @NotBlank\n    @Email\n    private String email;\n    \n    @NotBlank\n    @Size(min = 2, max = 50)\n    private String name;\n}\n```\n\n## application.yml\n```yaml\nspring:\n  datasource:\n    url: jdbc:postgresql://localhost:5432/mydb\n    username: user\n    password: pass\n  jpa:\n    hibernate:\n      ddl-auto: update\n    show-sql: true\n\nserver:\n  port: 8080\n```\n\n## 部署\n```bash\n./gradlew build\njava -jar build/libs/my-app.jar\n\n# Docker\nFROM eclipse-temurin:21\nCOPY build/libs/*.jar app.jar\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]\n```\n\n## 心法\n- Spring Boot 自動配置魔法\n- DI（@Autowired）注入\n- JPA 不寫 SQL（但要懂）\n- application.yml 環境分離\n\n**☕ 用人話講**\n\nSpring Boot 起步——**5 分鐘就能建出一個企業級 API**，它把 Java 後端原本繁瑣的設定全包好了。**一句話記住**：像買一整套配好的廚具組——不用一件件去配，Spring Boot 把企業後端需要的都準備好，你直接開火做菜（寫業務邏輯）。\n",
      "tip": {
        "type": "milestone",
        "text": "Spring Boot = Java 工程師標準。"
      },
      "id": "34.2",
      "number": "LESSON 34.2",
      "xp": 10,
      "playgrounds": [
        {
          "key": "34.2.auto",
          "language": "java",
          "title": "試試看",
          "initialCode": "@SpringBootApplication\n@RestController\npublic class App {\n    public static void main(String[] args) {\n        SpringApplication.run(App.class, args);\n    }\n    \n    @GetMapping(\"/\")\n    public String hello() {\n        return \"Hello, World!\";\n    }\n}",
          "hint": "改 code、按執行看結果"
        }
      ],
      "exercise": {
        "question": "Spring Boot 的『約定優於配置 + 自動配置 + 內嵌伺服器』讓你幾行就跑起一個 REST API——它幫你省掉了傳統 Java/Spring 的什麼繁瑣(XML 配置、手動組裝、外部容器)？@RestController/@GetMapping 這種註解驅動的好處與『魔法太多不知背後發生什麼』的代價各是什麼？",
        "hint": "Boot 自動配置+內嵌 server 省繁瑣;註解方便但藏行為。",
        "answer": "Spring Boot 用自動配置(依 classpath 猜你要什麼並配好)、內嵌 Tomcat(不用另裝部署到外部容器)、starter 依賴(一個依賴帶齊一組)——省掉傳統 Spring 的大量 XML 配置與手動組裝 bean。@RestController+@GetMapping 註解驅動:宣告式、程式碼少、聚焦業務。代價:自動配置與註解是『魔法』——出問題或要客製時,不了解背後(它幫你配了什麼、bean 怎麼組)會難除錯;所以方便之外仍要理解 Spring 的核心機制(DI、bean 生命週期)才能駕馭。"
      },
      "outline": [
        {
          "level": 2,
          "text": "起步"
        },
        {
          "level": 2,
          "text": "Hello World"
        },
        {
          "level": 2,
          "text": "REST Controller"
        },
        {
          "level": 2,
          "text": "JPA Entity"
        },
        {
          "level": 2,
          "text": "Repository"
        },
        {
          "level": 2,
          "text": "Service"
        },
        {
          "level": 2,
          "text": "Validation"
        },
        {
          "level": 2,
          "text": "application.yml"
        },
        {
          "level": 2,
          "text": "部署"
        },
        {
          "level": 2,
          "text": "心法"
        }
      ],
      "miniQuiz": {
        "question": "在 Spring Boot REST API 中，如果想讓 POST /api/users 創建使用者時回傳 HTTP 201 Created 狀態碼，應該在哪裡加上什麼註解？",
        "options": [
          {
            "value": "a",
            "label": "在 UserService 的 create() 方法上加 @ResponseStatus(CREATED)"
          },
          {
            "value": "b",
            "label": "在 UserController 的 create() 方法上加 @ResponseStatus(CREATED)"
          },
          {
            "value": "c",
            "label": "在 User Entity 的 @PostMapping 上加 @ResponseStatus(CREATED)"
          },
          {
            "value": "d",
            "label": "在 build.gradle 中設定 spring.http.encoding.enabled=true"
          }
        ],
        "answer": "b",
        "explanation": "@ResponseStatus 是控制器層的註解，用來設定 HTTP 回應狀態碼，所以要放在 UserController 的 @PostMapping 方法上；Service 層負責業務邏輯、Entity 是資料模型，都不是放狀態碼的地方。"
      }
    },
    {
      "title": "Spring Boot 進階",
      "oneLineSummary": "Auth / Migration / Test / 部署",
      "analogy": "寫 production-grade Java 必懂。",
      "content": "## Spring Security（Auth）\n```java\n@Configuration\n@EnableWebSecurity\npublic class SecurityConfig {\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        http.csrf(csrf -> csrf.disable())\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers(\"/api/public/**\").permitAll()\n                .anyRequest().authenticated()\n            )\n            .oauth2ResourceServer(oauth2 -> oauth2.jwt());\n        return http.build();\n    }\n}\n```\n\n## JWT\n```java\n@RestController\npublic class AuthController {\n    @PostMapping(\"/login\")\n    public Map<String, String> login(@RequestBody LoginDto dto) {\n        User user = userService.authenticate(dto.email, dto.password);\n        String token = jwtService.generate(user);\n        return Map.of(\"token\", token);\n    }\n}\n```\n\n## Flyway / Liquibase（Migration）\n```sql\n-- db/migration/V1__create_users.sql\nCREATE TABLE users (\n  id BIGSERIAL PRIMARY KEY,\n  email VARCHAR(255) UNIQUE NOT NULL,\n  name VARCHAR(100),\n  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n);\n```\n\n啟動自動 migrate。\n\n## Exception Handling\n```java\n@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(EntityNotFoundException.class)\n    public ResponseEntity<ErrorResponse> handleNotFound(EntityNotFoundException ex) {\n        return ResponseEntity.status(NOT_FOUND).body(new ErrorResponse(ex.getMessage()));\n    }\n    \n    @ExceptionHandler(MethodArgumentNotValidException.class)\n    public ResponseEntity<Map<String, String>> handleValidation(MethodArgumentNotValidException ex) {\n        Map<String, String> errors = new HashMap<>();\n        ex.getBindingResult().getFieldErrors().forEach(error ->\n            errors.put(error.getField(), error.getDefaultMessage())\n        );\n        return ResponseEntity.badRequest().body(errors);\n    }\n}\n```\n\n## Testing\n```java\n@SpringBootTest\n@AutoConfigureMockMvc\nclass UserControllerTest {\n    @Autowired\n    private MockMvc mockMvc;\n    \n    @Test\n    void createUser_validInput_returns201() throws Exception {\n        mockMvc.perform(post(\"/api/users\")\n            .contentType(APPLICATION_JSON)\n            .content(\"\"\"\n                { \"email\": \"test@example.com\", \"name\": \"Test\" }\n            \"\"\"))\n            .andExpect(status().isCreated())\n            .andExpect(jsonPath(\"$.email\").value(\"test@example.com\"));\n    }\n}\n```\n\n## Virtual Threads（Java 21）\n```java\n@RestController\npublic class Controller {\n    @GetMapping(\"/slow\")\n    public String slow() throws InterruptedException {\n        Thread.sleep(1000);\n        return \"done\";\n    }\n}\n\n// application.yml\nspring.threads.virtual.enabled: true\n```\n\n每 request 一個 virtual thread、可同時 100k requests。\n\n## Async\n```java\n@Service\npublic class EmailService {\n    @Async\n    public CompletableFuture<Void> sendEmail(String to, String body) {\n        // 慢 IO\n        return CompletableFuture.completedFuture(null);\n    }\n}\n```\n\n## Cache\n```java\n@Cacheable(\"users\")\npublic User findById(Long id) {\n    return userRepository.findById(id).orElseThrow();\n}\n\n@CacheEvict(value = \"users\", key = \"#user.id\")\npublic User update(User user) {\n    return userRepository.save(user);\n}\n```\n\n## 部署\n- Docker container\n- Kubernetes\n- AWS Beanstalk\n- Heroku\n- Railway / Fly.io（也支援）\n\n## GraalVM Native Image\n編譯成 native binary：\n- 啟動快（< 100ms）\n- 記憶體少（10MB）\n- 適合 serverless\n\n```bash\n./gradlew nativeCompile\n```\n\n## 心法\n- Spring Boot 是 Java 標準\n- 學熟 = 大企業履歷強\n- Java 21 + Virtual Threads = 性能爆好\n- 跟 startup 比慢、跟企業比穩\n\n**☕ 用人話講**\n\nSpring Boot 進階——一個完整後端要的進階主題：**Auth（認證）、Migration（資料庫遷移）、Test（測試）、部署**。**一句話記住**：像房子蓋好後的進階裝修——裝門禁（auth）、升級水電（migration）、驗收（test）；把這些做好，專案才真的能上線營運。\n",
      "tip": {
        "type": "milestone",
        "text": "Java + Spring Boot = 進大企業 / 金融的入場券。"
      },
      "id": "34.3",
      "number": "LESSON 34.3",
      "xp": 10,
      "playgrounds": [
        {
          "key": "34.3.auto",
          "language": "java",
          "title": "試試看",
          "initialCode": "@Configuration\n@EnableWebSecurity\npublic class SecurityConfig {\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        http.csrf(csrf -> csrf.disable())\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers(\"/api/public/**\").permitAll()\n                .anyRequest().authenticated()\n            )\n            .oauth2ResourceServer(oauth2 -> oauth2.jwt());\n        return http.build();\n    }\n}",
          "hint": "改 code、按執行看結果"
        }
      ],
      "exercise": {
        "question": "Spring 的核心是『依賴注入(DI)/控制反轉(IoC)』——為什麼把物件的建立與組裝交給容器、而不是自己 new,能讓程式解耦、好測試(可注入 mock)？@Service/@Repository 分層與建構子注入(相比 field 注入)為什麼是推薦做法？",
        "hint": "DI 解耦、可注入 mock;建構子注入明確可測。",
        "answer": "DI/IoC:物件需要的依賴由容器注入,而非自己 new——你的類別只宣告『我需要什麼』,不管誰給、怎麼建。好處:解耦(依賴介面而非具體實作,可替換)、好測試(測試時注入 mock 依賴,不碰真 DB/外部)。@Service(商業邏輯)/@Repository(資料存取)分層讓職責清楚、可各自測。建構子注入(把依賴當建構子參數)優於 field 注入:依賴明確(一看建構子就知道要什麼)、可設 final(不可變)、測試時直接 new 傳入 mock 不需容器、且避免循環依賴悄悄成立。"
      },
      "outline": [
        {
          "level": 2,
          "text": "Spring Security（Auth）"
        },
        {
          "level": 2,
          "text": "JWT"
        },
        {
          "level": 2,
          "text": "Flyway / Liquibase（Migration）"
        },
        {
          "level": 2,
          "text": "Exception Handling"
        },
        {
          "level": 2,
          "text": "Testing"
        },
        {
          "level": 2,
          "text": "Virtual Threads（Java 21）"
        },
        {
          "level": 2,
          "text": "Async"
        },
        {
          "level": 2,
          "text": "Cache"
        },
        {
          "level": 2,
          "text": "部署"
        },
        {
          "level": 2,
          "text": "GraalVM Native Image"
        },
        {
          "level": 2,
          "text": "心法"
        }
      ],
      "miniQuiz": {
        "question": "在 Spring Boot 進階開發中，如果你想要保護 API 端點，讓只有通過身份驗證的使用者才能存取 `/api/private/**` 路徑，但允許所有人存取 `/api/public/**`，應該使用什麼來實現？",
        "options": [
          {
            "value": "a",
            "label": "在 SecurityConfig 中用 authorizeHttpRequests 設定不同路徑的存取權限"
          },
          {
            "value": "b",
            "label": "在每個 Controller 上面加 @Secured 註解，逐一設定權限"
          },
          {
            "value": "c",
            "label": "直接在 application.properties 設定哪些路徑需要認證"
          },
          {
            "value": "d",
            "label": "使用 Flyway 在資料庫層面設定使用者的存取權限"
          }
        ],
        "answer": "a",
        "explanation": "課程中的 SecurityConfig 示範用 authorizeHttpRequests 來管理不同 URL 的存取規則，這是 Spring Security 的標準做法。選項 b 是舊版方式、c 無法設定，d 是資料庫遷移工具與此無關。"
      }
    },
    {
      "title": "Spring Boot 是什麼",
      "oneLineSummary": "Java 生態最大後端 framework",
      "analogy": "金融 / 大企業必備。",
      "content": "## Spring Boot 是什麼\n\nSpring Boot 是什麼？簡單講，它是一個 **Java 後端框架**——幫助開發者快速建立企業級的網頁 API 和應用程式。如果你把 Java 看作「汽車的引擎」，Spring Boot 就是「把引擎裝進車子、加上方向盤和油門」的工程師。\n\n### 為什麼全世界企業都選它\n\n#### 1. 業界生態最成熟\nSpring Boot 在全球企業中使用最廣，尤其是**銀行、保險、電商、政府機構**。為什麼？因為：\n- 已經經過 20+ 年的考驗（Spring 框架本身從 2003 年開始）\n- 大公司專案裡的坑都被踩過、解決方案都現成\n- 人才多、找人容易、知識分享也多\n- 和現有的企業系統整合能力最強\n\n你如果看台灣銀行業徵才，**十個 Java 職位有八個都要 Spring / Spring Boot**。這代表什麼？代表學它，未來找工作會方便很多。\n\n#### 2. 開發速度快\n最核心的優點就四個字：**約定優於配置**（Convention over Configuration）。\n\n什麼意思呢？傳統 Java 開發要自己寫一堆設定檔（XML、properties），告訴框架「我的資料庫在這、連接池設定多少、路由怎麼走」。Spring Boot 反過來說：「你不用告訴我，我自動猜。你的資料庫 jar 包都放進去了，我就認定你要用它、自動幫你連上。你的 Controller 方法叫 `hello`，我就自動接 `/hello` 的請求。」\n\n這樣做的好處？**少寫 90% 的樣板程式（boilerplate code）**。你可以把時間花在真正有價值的業務邏輯上。\n\n#### 3. 內建應用伺服器\n以前 Java 開發，你要：\n1. 寫完程式碼\n2. 把它編譯成 `.war` 檔（Web Application aRchive）\n3. 手動部署到 Tomcat / JBoss / WebLogic 這種獨立的應用伺服器\n4. 啟動伺服器、看日誌\n\n現在 Spring Boot？一個指令就跑：\n\n🖥️\n```bash\n./mvnw spring-boot:run\n```\n\n完了。整個 Tomcat 伺服器已經打包在程式裡面了。你不用裝任何東西、不用配置 Tomcat，一行指令，10 秒後網頁 API 就活著了。這對開發速度的幫助有多大？**開發環境的啟動時間從「半小時」掉到「10 秒」**。\n\n#### 4. IDE 支援超好\nJetBrains 的 **IntelliJ IDEA**（Java 開發首選 IDE）和 Spring Boot 整合得完美。你在 IDE 裡新建 Spring Boot 專案、創建 Controller、寫 @Autowired 註解，IDE 會：\n- 自動完成類別名稱\n- 實時檢查 Bean 有沒有找到\n- 一鍵運行應用\n- Debug 時顯示 Spring 容器內的所有物件\n\n這種開發體驗比起手寫 XML、靠終端機操作，整個爽度不同級。\n\n#### 5. 社群又大又活躍\nStack Overflow 上 Spring Boot 的問題有幾十萬筆。遇到任何問題，99% 都有人問過、有解答。GitHub 上官方的 Spring Boot 專案，每年更新頻繁、文件完整。你學習的資源永遠不缺。\n\n---\n\n## 說實話：缺點也挺明顯\n\n#### 啟動時間慢\nSpring Boot 應用啟動需要 **10～30 秒**。為什麼？因為它要：\n- 掃描 classpath（所有 jar 包）\n- 自動偵測你需要什麼、把 Bean 都創建好\n- 連接資料庫、初始化連接池\n- 準備好所有 HTTP 路由\n\n如果你是開發「Serverless 函式」（比如 AWS Lambda、Google Cloud Functions），冷啟動 30 秒代表**每次呼叫都要等半分鐘**。這在雲端環境就很吃虧。這也是為什麼 Go、Node.js 在 Serverless 領域更受歡迎。\n\n#### 記憶體吃很多\n一個簡單的 Spring Boot 應用，光啟動就要 **200～400 MB 的 RAM**。如果你在一台小伺服器上跑 10 個 Spring Boot 應用，可能就吃滿了。這在成本敏感的項目（比如新創、或者邊緣運算場景）就是問題。\n\n#### 學習曲線陡\nSpring Boot 表面上「開箱即用」，但真的懂它、改配置、串接複雜的 ORM（Object-Relational Mapping）關係、處理事務（transaction），還是需要學很多東西。初學者如果沒有指導，容易被魔法般的自動配置搞糊塗。\n\n---\n\n## 快速起步：從零到 API\n\n### 方式 1：用 Spring Initializr（推薦）\n\n最簡單的方式就是上 **https://start.spring.io** 這個網站。打開後：\n1. 選語言：Java\n2. 選建置工具：Maven（或 Gradle，Maven 更常見）\n3. 選 Spring Boot 版本：最新穩定版（比如 3.2.x）\n4. 填 Group ID（組織代碼，比如 `com.mycompany`）、Artifact ID（專案名稱，比如 `myapi`）\n5. 加 Dependencies：選 `Spring Web`（做 REST API）、`Spring Data JPA`（資料庫）、`PostgreSQL Driver`（如果用 PostgreSQL）\n6. 按 Generate，下載 zip、解壓\n\n或者直接用指令：\n\n🖥️\n```bash\ncurl https://start.spring.io/starter.zip \\\n  -d type=maven-project \\\n  -d language=java \\\n  -d bootVersion=3.2.0 \\\n  -d groupId=com.mycompany \\\n  -d artifactId=myapi \\\n  -d dependencies=web,data-jpa,postgresql \\\n  -o myapi.zip\n\nunzip myapi.zip\ncd myapi\n```\n\n### 方式 2：用 IntelliJ（企業標配）\n打開 IntelliJ → File → New → Project → Spring Initializr，直接在 IDE 裡走完整個流程。\n\n### 寫你的第一個 API\n\n解壓後的資料夾結構大概是：\n```\nmyapi/\n├── src/main/java/com/mycompany/myapi/\n│   └── MyapiApplication.java       ← 程式進入點\n├── src/main/resources/\n│   └── application.properties       ← 設定檔\n├── pom.xml                          ← Maven 依賴清單\n└── mvnw                             ← Maven 包裝器（不用裝 Maven）\n```\n\n開啟 `src/main/java/com/mycompany/myapi/`，新建一個檔案 `HelloController.java`：\n\n📄 `HelloController.java`\n```java\npackage com.mycompany.myapi;\n\nimport org.springframework.web.bind.annotation.*;\nimport java.util.Map;\n\n@RestController\n@RequestMapping(\"/api\")\npublic class HelloController {\n    \n    @GetMapping(\"/hello\")\n    public Map<String, String> hello() {\n        return Map.of(\"message\", \"Hello, World!\", \"status\", \"OK\");\n    }\n    \n    @GetMapping(\"/hello/{name}\")\n    public Map<String, String> helloName(@PathVariable String name) {\n        return Map.of(\"message\", \"Hello, \" + name + \"!\");\n    }\n}\n```\n\n解釋一下這段程式碼：\n- `@RestController` ——這個類別就是 REST API 的控制器，會自動把 return 的東西變成 JSON 傳回去\n- `@RequestMapping(\"/api\")` ——所有這個類別的方法，網址都會加上 `/api` 前綴\n- `@GetMapping(\"/hello\")` ——HTTP GET 請求的路由，網址會是 `/api/hello`\n- `@PathVariable String name` ——把網址上的 `{name}` 參數取出來\n\n現在執行：\n\n🖥️\n```bash\n./mvnw spring-boot:run\n```\n\n稍等 10～15 秒，會看到：\n\n💬\n```\n...\nStarted MyapiApplication in 12.345 seconds\n```\n\n然後用瀏覽器或 curl 測試：\n\n🖥️\n```bash\ncurl http://localhost:8080/api/hello\n```\n\n💬\n```json\n{\"message\":\"Hello, World!\",\"status\":\"OK\"}\n```\n\n🖥️\n```bash\ncurl http://localhost:8080/api/hello/Alice\n```\n\n💬\n```json\n{\"message\":\"Hello, Alice!\"}\n```\n\n完成！你剛才用 Spring Boot 寫出的 API 就活著了。\n\n---\n\n## 核心概念 1：依賴注入（Dependency Injection）\n\nSpring 最強的魔法就是「自動給你注入物件」。\n\n比如，你寫了一個 `UserService` 類別，它內部需要一個 `UserRepository` 來查詢資料庫：\n\n📄 `UserService.java`\n```java\npackage com.mycompany.myapi;\n\nimport org.springframework.stereotype.Service;\n\n@Service\npublic class UserService {\n    private final UserRepository repo;\n    \n    // Spring 會自動呼叫這個建構子、把 UserRepository 注進去\n    public UserService(UserRepository repo) {\n        this.repo = repo;\n    }\n    \n    public User findUserByEmail(String email) {\n        return repo.findByEmail(email).orElse(null);\n    }\n}\n```\n\n你不用在 main 裡手寫 `new UserService(new UserRepository())`。Spring 啟動時會自動掃描所有 `@Service`、`@Repository`、`@Component` 的類別，把它們全部建立一份、放進「容器」（Spring Container）裡。當某個類別需要它們，Spring 就自動注進去。\n\n這叫 **IoC（Inversion of Control —— 控制反轉）**——你不再主動去 `new` 出每一個物件、再手動組裝它們；而是「反過來」——由 Spring 容器幫你建立、管理、注入。你只要在建構子宣告「我需要什麼」，Spring 就把對應的物件送進來。\n\n**為什麼這樣比較好？**\n- **好測試**：測試時可以塞一個「假的」`UserRepository`（mock）進去，不用真的連資料庫。\n- **好替換**：今天用 MySQL、明天換 PostgreSQL，只要換注入的實作，其他程式碼不用動。\n- **好維護**：物件之間的關係集中由容器管理，不會散落一地的 `new`。\n\n---\n\n## 核心概念 2：JPA（把資料庫當物件用）\n\n**JPA（Java Persistence API —— Java 持久化 API）** 讓你不用手寫 SQL，直接用 Java 物件操作資料庫。Spring Boot 底層用 Hibernate 幫你把物件翻譯成 SQL。\n\n先定義一個對應資料表的類別（Entity）：\n\n📄 `User.java`\n```java\npackage com.mycompany.myapi;\n\nimport jakarta.persistence.*;\n\n@Entity\n@Table(name = \"users\")\npublic class User {\n    @Id\n    @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long id;\n\n    @Column(unique = true, nullable = false)\n    private String email;\n\n    private String name;\n\n    // getters / setters 省略\n}\n```\n\n再定義一個 Repository 介面——你「只宣告方法名」，Spring 自動幫你產 SQL：\n\n📄 `UserRepository.java`\n```java\npackage com.mycompany.myapi;\n\nimport org.springframework.data.jpa.repository.JpaRepository;\nimport java.util.Optional;\n\npublic interface UserRepository extends JpaRepository<User, Long> {\n    // Spring 看方法名自動生 SQL：SELECT * FROM users WHERE email = ?\n    Optional<User> findByEmail(String email);\n}\n```\n\n你完全沒寫任何一行 SQL、也沒寫實作，就有了「用 email 查使用者」的功能。`JpaRepository` 還內建 `save()`、`findById()`、`findAll()`、`delete()` 等常用操作。\n\n**背後發生什麼？** 你呼叫 `repo.findByEmail(\"a@b.com\")`，Hibernate 自動翻成 `SELECT * FROM users WHERE email = ?`、執行、把結果對應回 `User` 物件。你感受不到 SQL，就是物件操作。\n\n（提醒：JPA 方便，但要懂它何時發查詢——關聯用不好會有 N+1 問題，這在進階課會細講。）\n\n---\n\n## 心法\n\n1. **Spring Boot 的核心賣點是「約定優於配置 + 自動配置」**——幫你搞定瑣碎設定，讓你專心寫業務邏輯。\n2. **依賴注入（DI / IoC）是 Spring 的靈魂**——別自己 `new`，讓容器幫你組裝，程式才好測、好維護。\n3. **JPA 讓你用物件操作資料庫**——爽，但要懂它背後發的 SQL，別當黑魔法。\n4. **啟動慢、吃記憶體是代價**——Serverless / 極輕量場景要斟酌，但企業長駐服務完全不是問題。\n\n**☕ 用人話講**\n\nSpring Boot 是 Java 生態最大的後端框架，靠「自動配置」把繁瑣設定全包好。**一句話記住**：它像一間「已經裝潢好、水電全通」的店面——你不用自己拉線接管，開門就能開始做生意（寫業務邏輯）。缺點是「房子」比較重、開門（啟動）要等一下、電費（記憶體）高一點，但對要 24 小時穩定營運的企業來說，這些都值得。",
      "tip": {
        "type": "practical",
        "text": "Java 工作多但 Indie 不推、選 stack 看目標。"
      },
      "exercise": {
        "question": "JVM 有垃圾回收,但 Java 服務仍會遇到記憶體問題(記憶體洩漏、OOM、GC 停頓)——洩漏在有 GC 的語言怎麼發生(還被參照著就不回收,如靜態集合只增不減、忘了移除的監聽)？為什麼調 heap 大小與選 GC 對高吞吐/低延遲服務重要？",
        "hint": "GC 只回收沒人參照的;洩漏=忘了放手;heap/GC 調校影響延遲。",
        "answer": "GC 只回收『再也連不到』的物件;洩漏是『你以為不用了、但還有引用鏈連著』——如靜態 Map/List 只加不刪、快取無上限成長、忘了反註冊的監聽器/回呼,這些讓物件一直被參照、GC 收不掉,最終 OOM。調校重要:heap 太小頻繁 GC/OOM、太大 GC 停頓長;不同 GC(如低延遲的 ZGC/G1)在『高吞吐 vs 低停頓』間取捨——對延遲敏感服務要選對 GC 並調 heap,才不會因 GC 停頓造成請求卡頓。用 heap dump/profiler 定位洩漏。"
      },
      "id": "34.4",
      "number": "LESSON 34.4",
      "xp": 10,
      "playgrounds": [
        {
          "key": "34.4.auto",
          "language": "java",
          "title": "試試看",
          "initialCode": "@RestController\n@RequestMapping(\"/api\")\npublic class HelloController {\n    @GetMapping(\"/hello\")\n    public Map<String, String> hello() {\n        return Map.of(\"message\", \"Hello, World!\");\n    }\n}",
          "hint": "改 code、按執行看結果"
        }
      ],
      "outline": [
        {
          "level": 2,
          "text": "為什麼用 Spring Boot"
        },
        {
          "level": 3,
          "text": "生態"
        },
        {
          "level": 3,
          "text": "優點"
        },
        {
          "level": 3,
          "text": "缺點"
        },
        {
          "level": 2,
          "text": "快速起步"
        },
        {
          "level": 3,
          "text": "Hello World"
        },
        {
          "level": 2,
          "text": "核心概念"
        },
        {
          "level": 3,
          "text": "Dependency Injection"
        },
        {
          "level": 3,
          "text": "JPA（DB）"
        },
        {
          "level": 2,
          "text": "心法"
        }
      ],
      "miniQuiz": {
        "question": "根據這堂課，Spring Boot 最大的優勢是什麼？",
        "options": [
          {
            "value": "a",
            "label": "啟動速度快，只需要 1-2 秒"
          },
          {
            "value": "b",
            "label": "用約定優於配置的方式，自動幫你搞定很多瑣碎設定，讓你專心寫業務邏輯"
          },
          {
            "value": "c",
            "label": "記憶體占用最低，很適合所有規模的專案"
          },
          {
            "value": "d",
            "label": "是目前唯一支援 serverless 架構的 Java 框架"
          }
        ],
        "answer": "b",
        "explanation": "課程強調 Spring Boot 的核心優勢是「約定 > 配置」和內建 server，讓開發者不用做大量的手動設定，直接專注業務邏輯。a 是缺點（啟動慢），c 和 d 都是課程明確提到的缺點或不適用情況。"
      }
    },
    {
      "title": "Spring Security + JPA 進階",
      "oneLineSummary": "認證 + 資料層、Spring 兩大支柱",
      "analogy": "懂這兩個 = 能做 80% 後端。",
      "content": "## Spring Security + JPA 進階\n\n### Spring Security 是什麼？為什麼企業一定要用\n\n想像你的銀行 App。每次登入、轉帳、看帳單，系統都要確認「你真的是你」、「你有沒有權限看這筆資料」。**Spring Security（Spring 安全框架）** —— 就是替你自動做這些檢查的工具。它處理三件事：\n\n1. **認證（Authentication）**—— 確認你是誰（帳號密碼、指紋、人臉）\n2. **授權（Authorization）**—— 確認你能做什麼（管理員可刪用戶、普通員工不行）\n3. **攻擊防禦**—— 擋掉常見網路攻擊（CSRF、跨域偽造等）\n\n為什麼要用現成框架而不自己寫？因為**安全不是自己寫加密函式就安全了**、一堆邊邊角角很容易遺漏、Spring Security 由安全專家維護、有數百萬企業驗證過。\n\n---\n\n### 設定 Security 的門禁規則\n\n```java\n@Configuration\n@EnableWebSecurity\npublic class SecurityConfig {\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        http\n            // 先關掉 CSRF（跨站偽造請求）防護\n            // 因為前後端分離、用 JWT token、已經不需要 CSRF cookie 防護\n            .csrf(csrf -> csrf.disable())\n            \n            // 設定哪些路徑誰能進去\n            .authorizeHttpRequests(auth -> auth\n                // 公開路徑：登入、註冊、文件 → 任何人都可以\n                .requestMatchers(\"/api/public/**\").permitAll()\n                .requestMatchers(\"/api/auth/login\").permitAll()\n                .requestMatchers(\"/api/auth/register\").permitAll()\n                \n                // 後台路徑：只有管理員可以進去\n                .requestMatchers(\"/api/admin/**\").hasRole(\"ADMIN\")\n                \n                // 其他所有請求：都要登入（通過認證）\n                .anyRequest().authenticated()\n            )\n            \n            // 告訴 Security：我用 JWT token 驗證身份（不用 Session）\n            .oauth2ResourceServer(oauth -> oauth.jwt());\n        \n        return http.build();\n    }\n}\n```\n\n**讀這段代碼時想像一道門：**\n- `/api/public/*` 路徑—— 門打開、誰都進\n- `/api/admin/*` 路徑—— 門有警衛、要檢查你是不是管理員\n- 其他路徑—— 門半開、要檢查你登過沒\n\n**常見誤區**：有人把密碼直接寫在代碼裡配置、那不叫安全。正確做法是用環境變數或密鑰管理系統（如 AWS Secrets Manager）。\n\n---\n\n### JWT Token（通行證）怎麼驗\n\n**JWT（JSON Web Token）** —— 把用戶身份資訊加密打包成一個字串，用戶每次請求都帶著它，後端驗證它沒被竄改就相信用戶身份。\n\n```java\n@RestController\npublic class ProfileController {\n    \n    // 取得當前登入用戶的個人資料\n    @GetMapping(\"/api/me\")\n    public Map<String, Object> me(@AuthenticationPrincipal Jwt jwt) {\n        // @AuthenticationPrincipal Jwt jwt\n        // Spring Security 會自動解析 request header 裡的 token\n        // 如果 token 無效 → 直接返回 401 Unauthorized、不會進來這個方法\n        \n        return Map.of(\n            \"userId\", jwt.getSubject(),           // token 裡的 sub 欄位（通常是用戶 ID）\n            \"email\", jwt.getClaim(\"email\"),       // token 裡的自訂欄位\n            \"roles\", jwt.getClaimAsStringList(\"roles\") // 用戶角色清單\n        );\n    }\n    \n    // 只有管理員能進的端點\n    @GetMapping(\"/api/admin/users\")\n    @PreAuthorize(\"hasRole('ADMIN')\")  // 用註解寫死權限檢查、比在 SecurityConfig 更靈活\n    public List<User> listAllUsers() {\n        return userRepo.findAll();\n    }\n}\n```\n\n**實際請求會長這樣：**\n\n🖥️ 假設客戶端已經登入、拿到 token\n\n```bash\ncurl -H \"Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...\" \\\n     http://localhost:8080/api/me\n```\n\n💬 返回\n\n```json\n{\n  \"userId\": \"12345\",\n  \"email\": \"user@bank.com\",\n  \"roles\": [\"CUSTOMER\"]\n}\n```\n\n**為什麼用 JWT 而不用 Session**：JWT 無狀態（後端不用存它、省資料庫）、適合分散系統、移動端也用得順。缺點是一旦 token 發出去、在過期前都無法立即撤銷（像飛機票一樣）。\n\n---\n\n### JPA Repository：讓 Java 替你寫 SQL\n\n**JPA（Java Persistence API）** —— 讓你用面向物件的語言操作資料庫、不用手寫 SQL。它自動翻譯成 SQL 指令。\n\n#### 1. 方法名自動產 SQL\n\n```java\npublic interface UserRepository extends JpaRepository<User, Long> {\n    // 方法名規則：find + By + 欄位名 + 條件\n    // Spring 看到方法名、自動產對應 SQL\n    \n    // SELECT * FROM users WHERE role = ?\n    List<User> findByRole(String role);\n    \n    // SELECT * FROM users WHERE created_at > ?\n    List<User> findByCreatedAtAfter(LocalDateTime date);\n    \n    // SELECT * FROM users WHERE email = ? (case insensitive)\n    Optional<User> findFirstByEmailIgnoreCase(String email);\n    \n    // SELECT * FROM users WHERE active = true AND role = ? ORDER BY created_at DESC\n    List<User> findByActiveAndRoleOrderByCreatedAtDesc(boolean active, String role);\n}\n```\n\n**好處**：簡潔、自動檢查、易讀；**缺點**：方法名太長時難维護、複雜邏輯寫不出來。\n\n#### 2. 手寫查詢 @Query\n\n當方法名太複雜、或需要 JOIN 其他表格時：\n\n```java\npublic interface UserRepository extends JpaRepository<User, Long> {\n    \n    // JPQL（Java Persistence Query Language）—— 用 Java 物件名、不是表名\n    @Query(\"SELECT u FROM User u WHERE u.email = :email AND u.active = true\")\n    Optional<User> findActiveByEmail(@Param(\"email\") String email);\n    \n    // 查詢搭配計數\n    @Query(\"SELECT COUNT(u) FROM User u WHERE u.role = :role\")\n    long countByRole(@Param(\"role\") String role);\n    \n    // 複雜 JOIN：查出每個用戶的最新訂單\n    @Query(\"SELECT new map(u.id as userId, u.email as email, o.id as orderId, o.amount as amount) \" +\n           \"FROM User u LEFT JOIN Order o ON u.id = o.userId \" +\n           \"WHERE u.active = true\")\n    List<Map<String, Object>> getUsersWithOrders();\n}\n```\n\n#### 3. 原生 SQL（最後手段）\n\n某些複雜查詢（如用資料庫特定語法）無法用 JPQL 表達時：\n\n```java\npublic interface UserRepository extends JpaRepository<User, Long> {\n    \n    // nativeQuery = true 表示直接執行 SQL、不翻譯\n    @Query(value = \"SELECT u.* FROM users u \" +\n                   \"WHERE DATE(u.created_at) = CURDATE() \" +\n                   \"AND u.verified = 1 \" +\n                   \"ORDER BY u.created_at DESC\", \n           nativeQuery = true)\n    List<User> findTodayVerifiedUsers();\n    \n    // 還可以搭配分頁\n    @Query(value = \"SELECT * FROM users WHERE status = ?1\", \n           nativeQuery = true)\n    Page<User> findByStatus(String status, Pageable pageable);\n}\n```\n\n**原生 SQL 的危險**：寫錯 SQL、SQL 注入攻擊、換資料庫時要改程式。盡量用 JPQL。\n\n---\n\n### N+1 問題（最常踩的坑）\n\n假設 User 和 Order 是一對多關係（一個用戶可以有多個訂單）：\n\n```java\n// ❌ 錯誤做法\n@Service\npublic class UserService {\n    \n    public List<UserDTO> getAllUsersWithOrders() {\n        List<User> users = userRepo.findAll();  // 1 句 SQL：取所有用戶\n        \n        for (User user : users) {\n            user.getOrders();  // ← 每個用戶都執行 1 句 SQL！ \n                               // 100 個用戶 = 1 + 100 = 101 句 SQL（N+1 問題）\n        }\n        \n        return users.stream().map(this::toDTO).collect(toList());\n    }\n}\n```\n\n**為什麼慢**：資料庫來回通訊要時間；100 次來回比 1 次來回慢 100 倍。\n\n```java\n// ✅ 正確做法 1：@EntityGraph（推薦簡單場景）\npublic interface UserRepository extends JpaRepository<User, Long> {\n    \n    // 告訴 JPA：取 User 時一起 eager load orders 關聯\n    @EntityGraph(attributePaths = {\"orders\"})\n    List<User> findAll();\n}\n\n// ✅ 正確做法 2：JOIN FETCH（推薦複雜查詢）\n@Query(\"SELECT DISTINCT u FROM User u LEFT JOIN FETCH u.orders WHERE u.active = true\")\nList<User> findAllActiveWithOrders();\n\n// ✅ 正確做法 3：只查需要的欄位（最快）\n@Query(\"SELECT new com.example.dto.UserOrderDTO(u.id, u.email, o.id, o.amount) \" +\n       \"FROM User u LEFT JOIN Order o ON u.id = o.userId\")\nList<UserOrderDTO> findUsersOrdersDTO();\n```\n\n---\n\n### Transaction：多步驟操作的原子性\n\n**Transaction（交易）** —— 確保多個資料庫操作要麼全部成功、要麼全部失敗、不會出現中間狀態。\n\n```java\n@Service\npublic class OrderService {\n    @Autowired private OrderRepository orderRepo;\n    @Autowired private PaymentService paymentService;\n\n    // @Transactional：這個方法裡的所有資料庫操作綁成「一筆交易」\n    // 任何一步拋出例外 → 全部 rollback（撤銷），不會只做一半\n    @Transactional\n    public Order placeOrder(CreateOrderDto dto) {\n        Order order = new Order(dto);\n        orderRepo.save(order);            // 步驟 1：先存訂單\n\n        paymentService.charge(order);     // 步驟 2：扣款（若這裡拋例外…）\n\n        // …上面 save() 的訂單也會被一起 rollback，資料庫回到動作前的乾淨狀態。\n        return order;\n    }\n}\n```\n\n**為什麼重要？** 想像沒有 Transaction：訂單存進去了、但扣款失敗——結果資料庫多了一筆「沒付錢的訂單」，帳就亂了。`@Transactional` 保證「要麼訂單 + 扣款都成功、要麼兩者都不留下」，這就是**原子性（Atomicity）**——一組操作是「不可分割」的一個整體。\n\n**常見雷**：\n- `@Transactional` 預設只對「執行期例外（RuntimeException）」rollback；受檢例外（checked exception）預設不會 rollback，需要時加 `@Transactional(rollbackFor = Exception.class)`。\n- 同一個類別內「自己呼叫自己」的方法，`@Transactional` 可能不生效（因為 Spring 靠代理攔截）——把交易方法放到另一個 Bean 呼叫比較保險。\n\n---\n\n## 心法\n\n1. **Spring Security 管「你是誰、你能做什麼」**——認證 + 授權分開想，門禁規則集中在 `SecurityConfig`。\n2. **JWT 無狀態、省 Session，但發出去在過期前很難即時撤銷**——要即時登出得另做黑名單。\n3. **JPA 方法名能自動產 SQL，複雜查詢用 `@Query`，最後才動原生 SQL**——原生 SQL 小心注入。\n4. **N+1 是最常踩的坑**——用 `JOIN FETCH` / `@EntityGraph` 一次撈關聯。\n5. **多步驟寫入一定包 `@Transactional`**——要麼全成、要麼全退，別留半套資料。\n\n**☕ 用人話講**\n\nSpring Security 和 JPA 是 Spring 後端的兩大支柱——一個管「安全（誰能進、能做什麼）」，一個管「資料（怎麼存、怎麼查）」。**一句話記住**：Security 是你家的門鎖 + 保全，JPA 是幫你把東西分門別類收好的智慧收納櫃。把這兩個搞熟，你就能做出企業級後端 80% 的核心功能。",
      "tip": {
        "type": "milestone",
        "text": "Spring 找工作必懂、自己 side project 不推。"
      },
      "exercise": {
        "question": "Spring Security 的認證(你是誰)與授權(你能做什麼)是分開的——用它保護 API 時,filter chain 在請求鏈哪個位置攔截、驗 token/session？JPA/Hibernate(ORM)方便但 N+1 與 lazy loading 陷阱怎麼發生、怎麼避免？為什麼『在 view 層才觸發 lazy 載入』會出 LazyInitializationException？",
        "hint": "Security filter 前置驗證;JPA N+1/lazy 陷阱;session 關了就載不到。",
        "answer": "Spring Security 用一串 filter 在請求到達 controller『之前』攔截:驗證身分(session/JWT)、再依角色/權限授權,失敗回 401/403。JPA/Hibernate N+1:查一組實體再對每個存取關聯各發一次查詢→用 fetch join 或 @EntityGraph 一次載入。lazy loading 陷阱:關聯設 lazy(用到才查),若在 transaction/session 關閉後(如已到 view 層)才存取,Hibernate 無 session 可查→LazyInitializationException;避免:在 service 層(session 還開著)就取好需要的資料、用 DTO 回傳、或明確 fetch。ORM 方便但要懂它何時發查詢。"
      },
      "id": "34.5",
      "number": "LESSON 34.5",
      "xp": 10,
      "playgrounds": [
        {
          "key": "34.5.auto",
          "language": "java",
          "title": "試試看",
          "initialCode": "@Configuration\n@EnableWebSecurity\npublic class SecurityConfig {\n    @Bean\n    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n        http\n            .csrf(csrf -> csrf.disable())\n            .authorizeHttpRequests(auth -> auth\n                .requestMatchers(\"/api/public/**\").permitAll()\n                .requestMatchers(\"/api/admin/**\").hasRole(\"ADMIN\")\n                .anyRequest().authenticated()\n            )\n            .oauth2ResourceServer(oauth -> oauth.jwt());\n        return http.build();\n    }\n}",
          "hint": "改 code、按執行看結果"
        }
      ],
      "outline": [
        {
          "level": 2,
          "text": "Spring Security"
        },
        {
          "level": 3,
          "text": "設定"
        },
        {
          "level": 3,
          "text": "JWT 驗證"
        },
        {
          "level": 2,
          "text": "JPA Query"
        },
        {
          "level": 3,
          "text": "Repository 方法名"
        },
        {
          "level": 3,
          "text": "自訂 query"
        },
        {
          "level": 3,
          "text": "Native SQL"
        },
        {
          "level": 2,
          "text": "Transaction"
        },
        {
          "level": 2,
          "text": "心法"
        }
      ],
      "miniQuiz": {
        "question": "根據課程內容，如果在 @Transactional 方法中，orderRepo.save() 成功了，但 paymentService.charge() 拋出異常，會發生什麼事？",
        "options": [
          {
            "value": "a",
            "label": "Order 會被保存到資料庫，因為 save() 已經執行成功"
          },
          {
            "value": "b",
            "label": "Order 會被 rollback，整個方法的資料庫操作都會被撤銷"
          },
          {
            "value": "c",
            "label": "只有 paymentService 的操作會失敗，其他服務不受影響"
          },
          {
            "value": "d",
            "label": "系統會自動重試 paymentService.charge() 直到成功"
          }
        ],
        "answer": "b",
        "explanation": "@Transactional 的核心就是：任一步失敗 → 全 rollback。當 paymentService.charge() 拋異常時，整個交易會被回滾，包括已經執行的 orderRepo.save()。這是事務的原子性原則。"
      },
      "demos": [
        {
          "type": "scenario-judge",
          "title": "Spring Security + JPA 判判看",
          "note": "認證 / JWT / JPA 查詢的最佳實踐 vs 反模式，判「可以 / 有風險 / 不行」。",
          "config": {
            "scenarios": [
              {
                "text": "前後端分離、改用 JWT 驗證後，把 Spring Security 的 CSRF 防護關掉",
                "correct": "ok",
                "why": "JWT 無狀態、不靠 cookie 帶身份、CSRF 的攻擊前提不成立，關掉是合理做法。"
              },
              {
                "text": "資料庫密碼直接寫死在 SecurityConfig 的程式碼裡",
                "correct": "no",
                "why": "機密進版控就形同外洩，密碼要放環境變數或密鑰管理（如 AWS Secrets Manager）。"
              },
              {
                "text": "用 @Query nativeQuery、把 request 參數字串串接進 SQL（... WHERE id = \" + req.id）",
                "correct": "no",
                "why": "這是典型 SQL injection 破口，一定要用參數綁定，別把使用者輸入接進 SQL 字串。"
              },
              {
                "text": "一對多關聯，先 findAll() 再在迴圈裡對每個 user 呼叫 getOrders()",
                "correct": "risk",
                "why": "這就是 N+1 問題：100 個用戶會多打 100 句 SQL，資料量一大就很慢。"
              },
              {
                "text": "改用 JOIN FETCH 或 @EntityGraph 一次把關聯一起撈出來",
                "correct": "ok",
                "why": "一句 SQL 解決關聯，正是修 N+1 的正解。"
              },
              {
                "text": "用 JWT 當登入憑證，卻要求「使用者按登出就立刻讓 token 失效」",
                "correct": "risk",
                "why": "JWT 無狀態、發出去在過期前無法即時撤銷；要即時登出得另做黑名單，別當它做得到。"
              },
              {
                "text": "Repository 用方法名 findByActiveAndRole 讓 Spring 自動產 SQL",
                "correct": "ok",
                "why": "方法名衍生查詢簡潔、可讀、編譯期檢查，簡單條件用它剛好。"
              }
            ]
          }
        }
      ]
    },
    {
      "title": "Spring Boot 部署 + 接案",
      "oneLineSummary": "上線 + 找客戶",
      "analogy": "Java 工作多、但啟動 Indie 慢。",
      "content": "## Spring Boot 部署 + 接案\n\n### 前言：為什麼要學部署？\n\n寫好程式只是第一步。程式要在「真實的電腦上、24 小時跑著」客戶才能用。部署（deploy —— 把程式送上線）就像你開了家餐廳，寫好食譜只是準備，真正要「開門營業」才能賺錢。Java 的部署方式很多，選錯會花冤枉錢、選對能省一大筆。\n\n---\n\n## 一、部署選擇——從本地到上線\n\n### JAR：最簡單的打包方式\n\nJAR（Java Archive —— Java 壓縮檔）就是把你寫的 Spring Boot 程式、加上所有依賴、全部打成一個檔案。執行它就能跑。\n\n**打包的指令**：\n```bash\n./mvnw clean package\n```\n\n這會在 `target/` 資料夾產生一個 `myapp.jar` 檔案（大小通常 50MB ~ 200MB）。\n\n**本地執行看看**：\n```bash\njava -jar target/myapp.jar\n```\n\n你會看到：\n```\n💬 . . .\n💬 2024-01-15 10:23:45 INFO  - Tomcat started on port(s): 8080 (http) with context path ''\n💬 2024-01-15 10:23:46 INFO  - Started MyApplication in 3.456 seconds\n```\n\n這時在瀏覽器打 `http://localhost:8080` 就能看到應用。\n\n**上線到 VPS**：最常見的方式是買台虛擬主機（VPS —— Virtual Private Server，幾百塊台幣一個月），裡面裝 Linux，把 JAR 檔案上傳上去，再執行它。這樣客戶就能 24 小時存取你的服務。\n\n```bash\n# 在你的電腦上\nscp target/myapp.jar user@your-vps-ip:/home/app/\n\n# 登入 VPS 後\nssh user@your-vps-ip\ncd /home/app\njava -jar myapp.jar\n```\n\n**前置條件**：VPS 要裝 Java（通常裝 Eclipse Temurin 或 OpenJDK）。\n\n---\n\n### Docker：讓部署變簡單\n\nDocker（容器化 —— 把程式和它的環境全部打進一個盒子）解決一個經典問題：「在我電腦上跑得好好的，為什麼上線就爆炸？」原因通常是環境不一樣（Java 版本、系統設定、依賴不同）。\n\n**Dockerfile**（一份部署配方）：\n```dockerfile\nFROM eclipse-temurin:21-jre-alpine\nCOPY target/myapp.jar /app.jar\nENTRYPOINT [\"java\", \"-jar\", \"/app.jar\"]\n```\n\n**拆解**：\n- `FROM eclipse-temurin:21-jre-alpine`——從一個官方的「包含 Java 21 的 Linux 環境」開始\n- `COPY target/myapp.jar /app.jar`——把你的 JAR 檔複製進去\n- `ENTRYPOINT [\"java\", \"-jar\", \"/app.jar\"]`——啟動時執行這個指令\n\n**構建 Docker 映像（image）**：\n```bash\ndocker build -t myapp:latest .\n```\n\n**本地執行**：\n```bash\ndocker run -p 8080:8080 myapp:latest\n```\n\n**為什麼選 alpine？** Alpine Linux 只有 5MB，比普通 Linux 小 20 倍，上傳和執行都快；如果碰到問題再改成 `eclipse-temurin:21-jre` 就好。\n\n**上線到 Docker 容器平台**：支援 Docker 的託管服務會自動幫你構建、執行、管理容器（包括自動重啟、流量分配等）。\n\n---\n\n### 託管平台選擇表\n\n| 平台 | 成本 | 難度 | 特色 | 適合對象 |\n|------|------|------|------|---------|\n| **Railway** | 月 USD 5～100 | ⭐ 最簡單 | 一個指令部署、自動 CI/CD | 新手、快速上線 |\n| **Fly.io** | 月 USD 3～50 | ⭐ 簡單 | 全球邊緣分發、效能好 | 需要全球訪問速度 |\n| **AWS Elastic Beanstalk** | 月 USD 10～200+ | ⭐⭐ 中等 | 自動擴展、與 AWS 其他服務整合 | 企業、大流量 |\n| **Google App Engine** | 月 USD 10～200+ | ⭐⭐ 中等 | Google 基礎設施、整合 Firebase | Google 生態 |\n| **Heroku** | 月 USD 7～500 | ⭐ 簡單 | 曾經最友善、現在 pricing 變貴 | 預算充足的小團隊 |\n| **Vercel** | ❌ | — | 不支援 Java（只支援 Node / Python 等） | 不選這個 |\n\n**給新手的建議**：Railway 或 Fly.io 選一個。部署真的只要：\n```bash\n# 裝 Railway CLI\nnpm install -g @railway/cli\n\n# 在你的專案資料夾執行\nrailway init\nrailway up\n```\n就完成了。\n\n---\n\n### 雲端 VM（自管模式）\n\n如果你想「完全掌控」、願意自己裝系統、設定防火牆、管理 SSL 憑證，可以直接租一台虛擬機：\n\n- **AWS EC2**（每月 USD 5～20）：業界標準，功能最全\n- **GCP Compute Engine**（每月 USD 5～20）：Google 的，類似 EC2\n- **Linode / DigitalOcean**（每月 USD 4～12）：較便宜，介面友善\n- **自管機房**：有預算 + 運維團隊才考慮\n\n**優點**：完全自由、長期便宜。\n**缺點**：要自己處理安全更新、備份、監控、擴展；一個人搞很辛苦。\n\n---\n\n## 二、效能優化——讓程式跑得更快\n\n### 問題：Spring Boot 啟動慢、吃記憶體多\n\n一個典型的 Spring Boot 應用啟動需要 5～15 秒，靜止時佔用 300MB～1GB 記憶體。對「無伺服器」（serverless）或頻繁重啟的場景，這是痛點。\n\n### 方案 1：GraalVM Native Image——編譯成原生程式\n\nGraalVM 是一個特殊的 Java 運行環境，能把 Java 程式直接編譯成作業系統的原生二進位檔（像 C 語言一樣）。\n\n**安裝 GraalVM**（複雜一點，但跟著官方文件走）：\n```bash\n# macOS\nbrew install oracle/tap/graalvm-jdk21\n\n# 或手動下載：https://www.graalvm.org/downloads/\n```\n\n**編譯 Native Image**：\n```bash\n./mvnw native:compile -Pnative\n```\n\n**結果**：\n- ✅ 啟動時間：**100ms ~ 500ms**（不是秒，是毫秒！）\n- ✅ 記憶體佔用：**50MB ~ 100MB**（砍掉 5 倍）\n- ✅ 檔案大小：**60MB** 左右（一個獨立執行檔，不需要裝 Java）\n\n**缺點**：\n- ❌ 編譯很慢（15～30 分鐘）\n- ❌ 不是所有 library 都支援（Reflection —— 反射，一些動態載入的功能會壞）\n- ❌ 設定複雜，除錯困難\n\n**什麼時候用**：\n- 無伺服器函數（AWS Lambda）\n- Kubernetes 容器密集部署\n- 邊緣運算裝置\n\n**簡單專案不用**：普通 REST API 用 Jar + JVM 就夠快了。\n\n---\n\n### 方案 2：JVM 調優——調整 Java 虛擬機參數\n\n不需要編譯成原生程式，直接告訴 JVM「我的記憶體預算是多少、用什麼垃圾回收策略」。\n\n```bash\njava -Xms512m -Xmx2g -XX:+UseG1GC -jar myapp.jar\n```\n\n**參數拆解**：\n- `-Xms512m`——最少預留 512MB 記憶體（s = start）\n- `-Xmx2g`——最多用 2GB 記憶體（x = max）；超過就 OOM（Out Of Memory）崩潰\n- `-XX:+UseG1GC`——用 G1 垃圾回收器（比默認的 Serial GC 快得多）\n\n**實務建議**：\n```bash\n# 小應用（部落格、API）\njava -Xms256m -Xmx512m -XX:+UseG1GC -jar app.jar\n\n# 中等應用（電商、CRM）\njava -Xms1g -Xmx2g -XX:+UseG1GC -jar app.jar\n\n# 大型應用（銀行系統）\njava -Xms4g -Xmx8g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -jar app.jar\n```\n\n**在 Docker 裡用**（最常見）：\n```dockerfile\nENV JAVA_OPTS=\"-Xms512m -Xmx1g -XX:+UseG1GC\"\nENTRYPOINT [\"sh\", \"-c\", \"java $JAVA_OPTS -jar /app.jar\"]\n```\n\n---\n\n## 三、接案現實——Java 接案的客户、報價、技能\n\n### Java 為什麼好接案（但限定客戶）？\n\nJava 在「**大公司、銀行、政府**」特別吃香。為什麼？因為這些機構：\n- 重視穩定性、不怕慢\n- 有 IT 預算、不怕貴\n- 喜歡長期維護合約（= 你能一直賺）\n- 內部系統都是 Java（= 找你改舊程式的單子多）\n\n### 典型客戶類型\n\nJava 接案 / 工作的客戶，幾乎都集中在「大而穩」的機構：\n\n- **銀行 / 保險 / 證券**：核心系統、內部後台、報表系統，通常是長期維護合約。\n- **政府 / 公家機關**：標案為主，重穩定與文件，導入慢但合約久。\n- **大型電商 / 電信 / 製造業**：訂單、金流、ERP、後台 API，量大、要能擴展。\n- **既有 Java 系統的維護與改版**：很多公司十年前就用 Java，找人接手改舊程式的需求穩定。\n\n這些客戶的共通點：**重穩定、有預算、偏好長期合作**——不太會找一個素人接短單，多半透過公司、團隊或人力派遣進場。\n\n### 報價（僅供參考、非保證）\n\nJava 案子多半不是「個人接散單」的形態，而是以「進團隊 / 專案」計。市場上常見的**概略區間**差很多，取決於你的經驗、案子難度、客戶預算與談判：\n\n- **企業專案的接案 / 外包工程師**：常以月費或日費計，資淺與資深差距很大。\n- **系統維護 / 改版**：看系統規模與急迫度報價，通常有長期維護的空間。\n\n**務實提醒**：這裡給的是「市場上大概怎麼算」，不是你一定拿得到的數字。能不能接到案、報得動多少，取決於作品、口碑、面試與溝通——請把它當方向感，別當收入承諾。\n\n### 客戶常用 stack\n\n接 Java 案，客戶端常見的技術組合大致是：\n\n- **框架**：Spring Boot（幾乎必備）、Spring Security、Spring Data JPA\n- **資料庫**：Oracle、MS SQL Server（金融老系統多）、PostgreSQL / MySQL\n- **建置 / 部署**：Maven / Gradle、Docker、Jenkins / GitLab CI、Kubernetes\n- **雲**：AWS / Azure / GCP，或企業自建機房\n- **前端**：多半是另一組人做（React / Angular / Vue），你負責後端 API\n\n看得出來——**Java 接案 = 進「有制度的團隊」做企業系統**，跟個人 Indie 快速 ship 是兩種世界。\n\n## 心法\n\n1. **部署先從簡單的來**：新手用 Railway / Fly.io 一個指令上線；要掌控再學 Docker、雲端 VM。\n2. **啟動慢 / 吃記憶體有解**：一般服務 JVM 調參就夠；Serverless / K8s 密集才上 GraalVM Native Image。\n3. **Java 接案 = 進企業 / 團隊**，不是個人 Indie 主場——想走個人快速接案，Node / PHP 通常更順。\n4. **報價區間只是參考**——真正決定收入的是作品、經驗與溝通，不是語言本身。\n\n**☕ 用人話講**\n\n部署就是把寫好的程式「送上線、24 小時跑給客戶用」。**一句話記住**：Java 上線的路有很多條——新手走 Railway / Fly.io（像叫代駕，省事）、老手走自管 VM（像自己開車，自由但要顧一切）。而 Java 接案的真相是：它是「進大公司、大團隊」的門票，不是一個人快速接散單賺錢的工具——認清這點，你才不會選錯路。",
      "tip": {
        "type": "business",
        "text": "Java 工作多 = 找全職好、Indie 不推。"
      },
      "exercise": {
        "question": "部署 Spring Boot(內嵌伺服器→打成可執行 jar→Docker)相比傳統丟 war 到外部容器簡化了什麼？為什麼測試分層(單元測 service、@WebMvcTest 測 controller、@SpringBootTest 整合測)別全用最重的整合測？Spring 的 profile(dev/prod 不同設定)解決什麼？",
        "hint": "可執行 jar 免外部容器;測試分層別全整合;profile 分環境。",
        "answer": "Spring Boot 打成含內嵌 Tomcat 的可執行 jar(java -jar 就跑)、再包 Docker——不用再裝外部 Tomcat/部署 war,部署大幅簡化、環境一致。測試分層:單元測 service 邏輯(快、mock 依賴)、@WebMvcTest 只載 web 層測 controller(不啟整個 context)、@SpringBootTest 啟完整 context 做整合測(慢、少用)——別全用最重的整合測(慢、脆),用金字塔。profile(application-dev/prod.yml):同一份程式碼依環境載不同設定(DB 連線、log level、密鑰來源),用 spring.profiles.active 切換——dev/staging/prod 各自設定、不用改程式。"
      },
      "id": "34.6",
      "number": "LESSON 34.6",
      "xp": 10,
      "playgrounds": [
        {
          "key": "34.6.auto",
          "language": "dockerfile",
          "title": "試試看",
          "initialCode": "FROM eclipse-temurin:21-jre-alpine\nCOPY target/myapp.jar /app.jar\nENTRYPOINT [\"java\", \"-jar\", \"/app.jar\"]",
          "hint": "改 code、按執行看結果"
        }
      ],
      "outline": [
        {
          "level": 2,
          "text": "部署選擇"
        },
        {
          "level": 3,
          "text": "1. JAR"
        },
        {
          "level": 3,
          "text": "2. Docker"
        },
        {
          "level": 3,
          "text": "3. 託管"
        },
        {
          "level": 3,
          "text": "4. 雲端 VM"
        },
        {
          "level": 2,
          "text": "性能優化"
        },
        {
          "level": 3,
          "text": "Native Image（GraalVM）"
        },
        {
          "level": 3,
          "text": "JVM tuning"
        },
        {
          "level": 2,
          "text": "接案"
        },
        {
          "level": 3,
          "text": "客戶類型"
        },
        {
          "level": 3,
          "text": "報價"
        },
        {
          "level": 3,
          "text": "客戶常用 stack"
        },
        {
          "level": 2,
          "text": "心法"
        }
      ],
      "miniQuiz": {
        "question": "根據課程內容，如果你是個人開發者想透過 Java 接案賺錢，應該怎麼看待這條路？",
        "options": [
          {
            "value": "a",
            "label": "Java 接案主要面向個人 SaaS 開發，學好 Spring Boot 就能獨立創業"
          },
          {
            "value": "b",
            "label": "Java 接案機會主要在企業和開發團隊，而同樣時間學 Go/Node 對個人 Indie 開發更有利"
          },
          {
            "value": "c",
            "label": "只要掌握 Docker 和雲端部署，個人開發者就能和企業競爭 Java 案件"
          },
          {
            "value": "d",
            "label": "Native Image 可以讓啟動速度快 10 倍，這是個人開發者搶 Java 案件的關鍵優勢"
          }
        ],
        "answer": "b",
        "explanation": "課程的核心心法強調：Java 接案 = 進企業/開發團隊，而非 Indie 工具。個人開發者學 Java 是為了「履歷」進企業，不是為了獨立接案；同樣投入時間，學 Go/Node 對想做個人 SaaS 的開發者更划算。"
      },
      "demos": [
        {
          "type": "decision-quiz",
          "title": "Spring Boot 部署放哪裡？",
          "note": "依你的技術底、流量、成本，配一個對的部署方案。",
          "config": {
            "intro": "程式寫好了、要 24 小時跑給客戶用——你的情況適合哪種部署？",
            "questions": [
              {
                "q": "你願意花在維運（裝系統、防火牆、備份）的心力？",
                "options": [
                  {
                    "label": "只想快點上線、越少維運越好",
                    "scores": {
                      "managed": 2
                    }
                  },
                  {
                    "label": "中等、碰得動 Docker / CI",
                    "scores": {
                      "enterprise": 1,
                      "managed": 1
                    }
                  },
                  {
                    "label": "老手、想自己掌控整台機器",
                    "scores": {
                      "selfvm": 2
                    }
                  }
                ]
              },
              {
                "q": "流量規模？",
                "options": [
                  {
                    "label": "小到中、穩定就好",
                    "scores": {
                      "managed": 2
                    }
                  },
                  {
                    "label": "大流量、要自動擴展",
                    "scores": {
                      "enterprise": 2
                    }
                  },
                  {
                    "label": "serverless / K8s 容器密集、常冷啟動",
                    "scores": {
                      "native": 2
                    }
                  }
                ]
              },
              {
                "q": "成本與掌控怎麼取捨？",
                "options": [
                  {
                    "label": "願付月費換省事",
                    "scores": {
                      "managed": 2
                    }
                  },
                  {
                    "label": "要跟 AWS / GCP 生態整合",
                    "scores": {
                      "enterprise": 2
                    }
                  },
                  {
                    "label": "長期最省、自己扛維運",
                    "scores": {
                      "selfvm": 2
                    }
                  }
                ]
              },
              {
                "q": "冷啟動時間敏感嗎？",
                "options": [
                  {
                    "label": "無所謂、是常駐服務",
                    "scores": {
                      "managed": 1,
                      "enterprise": 1
                    }
                  },
                  {
                    "label": "非常敏感（Lambda 每次呼叫都要快）",
                    "scores": {
                      "native": 2
                    }
                  }
                ]
              }
            ],
            "outcomes": {
              "managed": {
                "emoji": "🚀",
                "title": "Railway / Fly.io",
                "desc": "一個指令部署、自動 CI/CD、月費幾美金起。新手快速上線最省事，缺點是彈性與掌控不如自管。"
              },
              "enterprise": {
                "emoji": "🏢",
                "title": "AWS Beanstalk / GCP App Engine",
                "desc": "自動擴展、跟雲端生態無縫整合，適合企業大流量。缺點是費用較高、設定中等複雜。"
              },
              "selfvm": {
                "emoji": "🛠️",
                "title": "自管雲端 VM（EC2 / Linode / DO）",
                "desc": "完全自由、長期最便宜，適合想完全掌控的老手。缺點是安全更新、備份、監控都得自己扛。"
              },
              "native": {
                "emoji": "⚡",
                "title": "GraalVM Native Image",
                "desc": "啟動 100ms 級、記憶體砍 5 倍，適合 serverless / K8s 密集部署。缺點是編譯慢、部分 library 反射會壞、除錯難。"
              }
            }
          }
        }
      ]
    }
  ],
  "boss": {
    "name": "Java 老靈",
    "hp": 200,
    "emoji": "☕",
    "description": "JVM 是它的迷宮、Spring 是它的咒語"
  },
  "quiz": {
    "id": "ch34-quiz",
    "questions": [],
    "xpReward": 50,
    "perfectBonus": 50
  },
  "summary": [
    "Java 的穩定性、跨平台性和龐大生態，讓它至今仍是企業開發的首選語言。",
    "Spring Boot 透過自動配置和內嵌伺服器，大幅簡化了 Spring 框架的複雜度，讓你快速啟動專案。",
    "REST API 是現代應用溝通的標準方式，Spring Boot 提供簡潔的註解讓你輕鬆打造。",
    "Spring Security 和 JPA 分別負責使用者認證授權和資料庫操作，兩者搭配可以架構出安全又有效率的系統。",
    "部署和接案是實戰的最後一哩路，掌握 Docker、雲端平台和常見錯誤排查，才能真正獨立交付產品。"
  ],
  "faq": [
    {
      "q": "為什麼要學 Java，不直接學新興語言？",
      "a": "Java 在企業、銀行、金融等大型系統中仍是主流，學它的就業機會和薪水行情都比較穩定。而且一旦理解 Java 的思維，轉向其他語言會更快。"
    },
    {
      "q": "Spring Boot 和 Spring 有什麼差別？",
      "a": "Spring 是龐大的框架家族，需要手動配置很多東西；Spring Boot 則是 Spring 的加速版本，自動幫你做好大部分設定，讓你專注在寫商業邏輯。"
    },
    {
      "q": "學完這章真的能接案或找到工作嗎？",
      "a": "這章會給你企業開發的扎實基礎和動手能力，但實際接案或求職還需要投作品、累積經驗、面試練習，不會一步到位——重點是你具備了門檻。"
    }
  ]
}
