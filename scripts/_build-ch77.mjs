// 產生 Ch28a《機器學習入門》ch77.json（id 77 / sortIndex 28.5 → 顯示 Ch28a）— 25 課完整版
// 遵循 docs/ch26_beginner_friendly_spec_v0：術語英中對照 + 四種區塊標籤 + 預設讀者零基礎。
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const __dirname = path.dirname(fileURLToPath(import.meta.url));
const OUT = path.join(__dirname, "..", "src", "data", "chapters", "ch77.json");
const J = (lines) => lines.join("\n");

// 每課：{ t:標題, s:oneLine, a:analogy, c:[content lines], tip:{type,text}, ex:{question,answer}, xp }
const L = [
  {
    t: "機器學習到底是什麼？跟一般程式差在哪",
    s: "一般程式是「你寫規則、電腦照做」；機器學習是「你給資料 + 答案、電腦自己歸納出規則」。這就是 AI 會「自己學」的核心。",
    a: "教小孩認貓。一般寫程式 = 你列「有鬍鬚、尖耳朵、會喵」一條條規則告訴電腦；機器學習 = 你給他看一千張貓的照片、他自己看出「貓長什麼樣」、下次看到新貓也認得。",
    c: [
      "## 先搞懂三個常被混用的詞",
      "",
      "AI、機器學習、深度學習三者是「一層包一層」（像三個大小不同的圈圈套在一起）：",
      "",
      "- **AI（人工智慧，Artificial Intelligence）** —— 最大的圈、泛指「讓電腦做出看起來聰明的事」。",
      "- **machine learning（機器學習，ML）** —— AI 裡的一種做法：「不直接寫規則、讓電腦從資料自己學規則」。",
      "- **deep learning（深度學習，DL）** —— ML 裡更進階的一支、用神經網路（下一章 Ch28b 專講）。",
      "",
      "```text",
      "┌─────────────────────────────┐",
      "│ AI（人工智慧）              │  ← 最大",
      "│   ┌───────────────────────┐ │",
      "│   │ ML（機器學習）        │ │  ← 從資料學規則（這一章）",
      "│   │    ┌────────────────┐ │ │",
      "│   │    │ DL（深度學習） │ │ │  ← 用神經網路",
      "│   │    └────────────────┘ │ │",
      "│   └───────────────────────┘ │",
      "└─────────────────────────────┘",
      "```",
      "",
      "## 一般程式 vs 機器學習：差在「規則誰寫」",
      "",
      "```text",
      "【一般程式】 你寫好規則 ＋ 資料  →  電腦照規則跑  →  答案",
      "   例：if 體溫 > 38: print(\"發燒\")     ← 規則是你訂死的",
      "",
      "【機器學習】 資料 ＋ 答案  →  電腦自己找出規則  →  得到一個「模型」",
      "   例：給 1 萬筆「體溫 + 有沒有生病」、讓電腦自己學出判斷標準",
      "```",
      "",
      "- 術語 **model（模型）** —— 機器學習「學完之後」得到的成果。可以想成「一台學會某件事的機器」、丟新資料進去、它就吐出預測。",
      "",
      "> 💡 一句話分辨：**規則是你親手寫死的＝一般程式；規則是電腦看資料自己歸納的＝機器學習。**",
      "",
      "## 什麼時候該用 ML、什麼時候別用",
      "",
      "```text",
      "✅ 適合（規則太複雜、人寫不完）：認貓狗、推薦影片、垃圾信過濾、預測趨勢",
      "❌ 不適合（規則很明確）：算稅金、判斷奇偶數 —— 直接寫程式又快又準",
      "```",
      "",
      "> ⚠️ 新手誤會：以為「什麼都丟給 AI 學就好」。規則清楚的事、直接寫程式更可靠。",
      "",
      "**☕ 用人話講**",
      "",
      "機器學習就是「**不寫死規則、改用資料教電腦、讓它自己歸納出規則**」。一般程式是你當老師把規則寫給電腦；ML 是你丟一堆「題目 + 答案」、讓它自己看出關係、學成一個「模型」。規則太複雜寫不完的事交給 ML；規則清楚的事直接寫程式更好。",
    ],
    tip: { type: "practical", text: "別被「AI / ML / DL」繞暈：AI 是大圈（讓電腦變聰明）、ML 是中圈（從資料學規則）、DL 是小圈（用神經網路、Ch28b 講）。這一章專心搞懂 ML。" },
    ex: { question: "想 3 個你今天用過的 App 功能、判斷哪些背後可能是機器學習、哪些只是一般程式。", answer: "例：鬧鐘響（一般程式）、IG 推薦貼文（ML）、計算機 1+1（一般程式）、語音轉文字（ML）。判斷標準：規則是不是「寫得死、講得清楚」。" },
    xp: 15,
  },
  {
    t: "機器學習的三大類型：監督 / 非監督 / 強化",
    s: "ML 分三種：監督式（有標準答案教它）、非監督式（沒答案、自己找規律）、強化式（試錯拿獎勵）。入門九成題目都是監督式。",
    a: "監督式 = 有附答案的考古題；非監督式 = 給你一箱沒貼標籤的東西自己分類；強化式 = 打電動、做對加分做錯扣分、慢慢學會怎麼玩。",
    c: [
      "## 三種「學法」",
      "",
      "### 1. 監督式學習（supervised learning）—— 有標準答案",
      "- 術語 **supervised learning（監督式學習）** —— 訓練資料每筆都附「正確答案」、電腦對著答案學。",
      "- 術語 **feature（特徵）** —— 拿來判斷的線索（坪數、屋齡）；**label（標籤）** —— 正確答案（成交價）。",
      "- 又分兩種：**迴歸 regression**（預測數字、如房價）、**分類 classification**（預測類別、如貓/狗）。",
      "",
      "### 2. 非監督式學習（unsupervised learning）—— 沒有答案",
      "- 術語 **unsupervised learning** —— 資料沒有標準答案、讓電腦自己找出隱藏結構。",
      "- 術語 **clustering（分群）** —— 把相似的資料自動聚成一群群（如客戶分群）。",
      "",
      "### 3. 強化式學習（reinforcement learning）—— 試錯拿獎勵",
      "- 術語 **reinforcement learning** —— 做對給獎勵、做錯給懲罰、在一次次嘗試中學最佳策略（如 AlphaGo、遊戲 AI）。",
      "",
      "| 類型 | 有標準答案？ | 典型例子 |",
      "|---|---|---|",
      "| 監督式 | 有 | 預測房價、辨識貓狗、垃圾信 |",
      "| 非監督式 | 沒有 | 客戶分群、找相似商品 |",
      "| 強化式 | 用獎勵代替答案 | 下棋、機器人、遊戲 AI |",
      "",
      "## 入門先專心在「監督式」",
      "九成教學 / 比賽 / 接案都是監督式（有現成答案、最好上手）。本章後面也以監督式為主。",
      "",
      "**☕ 用人話講**",
      "",
      "ML 依「有沒有給答案」分三種：**監督式**有標準答案（最常用、像照考古題練）、**非監督式**沒答案（自己找規律）、**強化式**用獎懲代替答案（試錯學策略）。新手先把監督式練熟——它又分「預測數字的迴歸」和「預測類別的分類」。",
    ],
    tip: { type: "practical", text: "聽到題目先問：「有沒有現成的正確答案？」有 → 監督式（再看答案是數字還類別）；沒有、要自己找群 → 非監督式；靠試錯拿分 → 強化式。" },
    ex: { question: "判斷各屬哪類：(1) 用病歷預測會不會復發 (2) 把新聞自動分成幾個主題群 (3) 教 AI 玩瑪利歐拿高分。", answer: "(1) 監督式分類 (2) 非監督式分群 (3) 強化式。" },
    xp: 15,
  },
  {
    t: "一個完整的機器學習流程長什麼樣",
    s: "ML 不是「丟資料按一個鈕」。完整流程七步：定義問題 → 收資料 → 清理 → 切訓練/測試 → 訓練 → 評估 → 調整/部署。最花時間的是清資料、不是訓練。",
    a: "像準備考試：搞清楚考什麼 → 找教材 → 整理重點 → 留幾題當模擬考先別看答案 → 讀書練習 → 模擬考打分 → 弱點加強。",
    c: [
      "## 七個步驟",
      "",
      "```text",
      "① 定義問題   要預測什麼？數字（迴歸）還是類別（分類）？",
      "② 收集資料   data —— 資料量和品質決定模型上限",
      "③ 清理+特徵  把原始資料變成乾淨線索（最花時間）",
      "④ 切分資料   train/test split —— 一份教電腦、一份藏起來考它",
      "⑤ 選模型訓練 training —— 餵訓練資料、讓模型學規律",
      "⑥ 評估       用藏起來的測試資料打分",
      "⑦ 調整/部署  不夠好回頭調；夠好就 deploy 上線",
      "```",
      "",
      "- 術語 **feature engineering（特徵工程）**、**train/test split（訓練/測試集切分）**、**training（訓練）**、**evaluation（評估）**、**deploy（部署）** —— 後面幾課會一個個做。",
      "",
      "> 💡 真正關鍵是第 ⑥ 步——**用「它沒看過的資料」考、才知道是真學會、還是只是背題目。**",
      "",
      "## 哪一步最花時間？",
      "",
      "```text",
      "收資料 + 清理 + 特徵   ████████████████  ~75%",
      "選模型 + 訓練          ███               ~15%",
      "評估 + 調整 + 部署     ██                ~10%",
      "```",
      "",
      "> ⚠️ 業界鐵律 **garbage in, garbage out（垃圾進、垃圾出）**——資料爛、再強的模型也救不回來。",
      "",
      "**☕ 用人話講**",
      "",
      "ML 完整流程是「定義問題 → 收資料 → 清理 → 切訓練/測試 → 訓練 → 評估 → 調整/部署」。新手以為重點在訓練、其實訓練常常只有一行；真正花時間又決定成敗的是把資料弄乾淨。還有鐵律：一定要用「沒看過的資料」評估。",
    ],
    tip: { type: "practical", text: "做專案先別急著選厲害演算法。先把流程跑通一遍（哪怕最簡單的模型）、拿到第一個能評估的 baseline（基準線）、再優化——先求有、再求好。" },
    ex: { question: "用一句話說明：為什麼不能拿「訓練時用過的同一份資料」來幫模型打分？", answer: "因為模型可能只是把答案背起來、分數虛高。要用它沒看過的資料考、才知道是真學會還是死背——這就是要切測試集的原因。" },
    xp: 15,
  },
  {
    t: "你的第一個機器學習模型（scikit-learn）",
    s: "用 scikit-learn、3 個動作就能訓練第一個模型：建立模型 → .fit() 訓練 → .predict() 預測。我們做一個「看坪數預測房價」的小模型。",
    a: "scikit-learn 像「機器學習的懶人包」——別人把演算法都寫好了、你只要 fit（學）跟 predict（猜）兩個動作就能用。",
    c: [
      "## 先裝工具",
      "- 術語 **scikit-learn（簡稱 sklearn）** —— Python 最常用的機器學習套件、把幾十種演算法包成統一介面。",
      "",
      "🖥️ **在終端機輸入**（沿用 Ch26 的 uv）",
      "```text",
      "uv add scikit-learn",
      "```",
      "",
      "## 目標：看「坪數」預測「房價」（監督式 + 迴歸）",
      "",
      "📄 **寫進程式檔 house.py**",
      "```text",
      "from sklearn.linear_model import LinearRegression",
      "",
      "X = [[10], [20], [30], [40], [50]]   # 特徵：坪數（每筆用 [] 包住）",
      "y = [800, 1600, 2400, 3200, 4000]    # 答案：成交價（萬）",
      "",
      "model = LinearRegression()           # 建立模型（線性迴歸）",
      "model.fit(X, y)                      # 訓練：餵「線索 + 答案」、學規律",
      "",
      "guess = model.predict([[25]])        # 預測：25 坪大概多少？",
      "print(f\"25 坪預測房價：{guess[0]:.0f} 萬\")",
      "```",
      "",
      "**這段在做什麼？**",
      "- 術語 **fit（訓練）** —— 把資料餵給模型讓它學；**predict（預測）** —— 學完丟新資料叫它猜。",
      "- X 為什麼每筆都用 `[]` 包住？sklearn 規定輸入是「二維表格」（每筆一列、可以有多個特徵）。",
      "",
      "🖥️ **在終端機輸入**：`uv run house.py`",
      "",
      "💬 **電腦會顯示**",
      "```text",
      "25 坪預測房價：2000 萬",
      "```",
      "它沒看過 25 坪、卻能猜出 2000 萬——因為它學到「每坪約 80 萬」。**這就是學到規則。**",
      "",
      "## 換演算法只改一行",
      "```text",
      "from sklearn.tree import DecisionTreeRegressor",
      "model = DecisionTreeRegressor()   # 只改這行、fit/predict 完全一樣",
      "```",
      "",
      "> 💡 記住兩個動作就會用 sklearn 八成功能：**fit = 學、predict = 猜。**",
      "",
      "**☕ 用人話講**",
      "",
      "scikit-learn 三步跑出第一個模型：**建立 → `.fit()` 學 → `.predict()` 猜**。我們做的「坪數預測房價」就是監督式裡的迴歸。所有模型介面都一樣、想換演算法只改一行——這就是 sklearn 成為入門首選的原因。",
    ],
    tip: { type: "practical", text: "X（特徵）一定是二維（每筆用 [] 包）、y（答案）是一維。看到 Reshape your data 的錯誤、先檢查 X 是不是 [[…],[…]] 的形狀。" },
    ex: { question: "把 house.py 改成預測 60 坪、房價會是多少？先心算再跑程式對答案。", answer: "每坪約 80 萬、60 坪 ≈ 4800 萬。線性迴歸學到的就是「每坪 80 萬」這條直線。" },
    xp: 20,
  },
  {
    t: "train/test split：為什麼不能又教又考",
    s: "用同一份資料訓練又評估、模型會「背答案」拿假高分。要把資料切兩份：一份訓練、一份藏起來當考卷、才知道它對沒看過的資料準不準。",
    a: "老師上課就把考卷答案發給你背、考試又考一模一樣的題——你考 100 分、換新題就完蛋。train/test split 就是「留幾題不給你看、考試只考那幾題」。",
    c: [
      "## 問題：又當教材又當考卷 = 作弊",
      "用全部資料訓練、再用同一份打分、模型可能只是把答案背起來、分數虛高。",
      "- 術語 **overfitting（過擬合）** —— 模型把訓練資料背得太死、遇到新資料就不準（後面專講）。",
      "",
      "## 解法：切成訓練集 + 測試集",
      "- 術語 **train/test split** —— 隨機切兩份：常見 80% 訓練 + 20% 測試。訓練集教模型、測試集「藏到最後」才考。",
      "",
      "📄 **寫進程式檔**",
      "```text",
      "from sklearn.model_selection import train_test_split",
      "",
      "X_train, X_test, y_train, y_test = train_test_split(",
      "    X, y, test_size=0.2,    # 留 20% 當測試集（考卷）",
      "    random_state=42,        # 固定隨機種子、每次切法一樣（方便重現）",
      ")",
      "model.fit(X_train, y_train)             # 只用訓練集教",
      "score = model.score(X_test, y_test)     # 用沒看過的測試集打分",
      "print(f\"測試集分數：{score:.2f}\")",
      "```",
      "",
      "**這段在做什麼？**",
      "- 術語 **test_size=0.2**（留兩成測試）、**random_state（隨機種子，固定切法）**、**score（評分；迴歸常用 R²、分類常用 accuracy）**。",
      "",
      "> ⚠️ 最常見致命錯誤：切分前就用「全部資料」做清理 / 縮放、等於讓測試集資訊「漏」進訓練（**data leakage、資料外洩**）。順序永遠是「先切、再處理」。",
      "",
      "**☕ 用人話講**",
      "",
      "別用同一份資料又訓練又打分（模型會背答案、給假高分＝過擬合）。正確做法是 **train/test split**：訓練集教模型、測試集藏起來當考卷、用沒看過的資料打分才知真本事。鐵律：先切、再處理、避免資料外洩。",
    ],
    tip: { type: "warning", text: "最隱形的坑是 data leakage：切分前就用全部資料算平均 / 標準化 / 補缺失，都會漏資訊。習慣是「train_test_split 永遠放最前面、之後處理只 fit 訓練集」。" },
    ex: { question: "訓練集分數 0.98、測試集只有 0.55，代表什麼？怎麼辦？", answer: "典型過擬合：模型背得太死。處理：簡化模型、加資料、做正規化、或檢查 data leakage。重點是「兩個分數差很多」就是警訊。" },
    xp: 20,
  },
  {
    t: "資料清理：缺失值與異常值",
    s: "真實資料總是又髒又亂：有空格、有缺、有離譜的值。清理是 ML 的第一道工：缺失值要補或丟、異常值要偵測處理、不然模型學歪。",
    a: "做菜前洗菜挑爛葉。爛掉的（異常值）挑掉、缺的食材（缺失值）補上或這道不做——備料乾淨、菜才好吃。",
    c: [
      "## 真實資料一定很髒",
      "教科書的資料乾乾淨淨、真實世界滿是缺漏和錯誤。**清理不做、後面全白做。**",
      "",
      "## 缺失值（missing values）",
      "- 術語 **missing value（缺失值）** —— 某欄沒有資料（空白、NaN）。",
      "- 兩種處理：**補（impute）** 或 **丟（drop）**。",
      "",
      "```text",
      "import pandas as pd",
      "df[\"age\"].fillna(df[\"age\"].median(), inplace=True)  # 用中位數補（數值常用）",
      "df[\"city\"].fillna(df[\"city\"].mode()[0], inplace=True) # 用眾數補（類別常用）",
      "df.dropna(subset=[\"price\"], inplace=True)            # price 缺就整列丟",
      "```",
      "- 怎麼選：缺一點 → 補；缺超過一半、或那列關鍵欄位沒了 → 丟。",
      "",
      "## 異常值（outliers）",
      "- 術語 **outlier（異常值）** —— 離譜到不合理的值（年齡 200 歲、房價 0 元）。",
      "- 偵測：看分佈（盒鬚圖 / 描述統計）、或用 IQR / 標準差規則。",
      "",
      "```text",
      "print(df.describe())            # 先看 min / max 有沒有離譜",
      "q1, q3 = df[\"price\"].quantile([0.25, 0.75])",
      "iqr = q3 - q1",
      "mask = (df[\"price\"] >= q1 - 1.5*iqr) & (df[\"price\"] <= q3 + 1.5*iqr)",
      "df = df[mask]                   # 只留合理範圍",
      "```",
      "",
      "> ⚠️ 別無腦砍異常值。先想「它是錯誤、還是真的有這種極端情況？」（億萬豪宅是真的、不該砍）。",
      "",
      "**☕ 用人話講**",
      "",
      "真實資料又髒又亂、清理是第一關。**缺失值**看情況補（中位數 / 眾數）或丟整列；**異常值**先用描述統計和 IQR 揪出來、但要先判斷它是「打錯」還是「真的極端」再決定砍不砍。資料乾淨、模型才學得對。",
    ],
    tip: { type: "warning", text: "補缺失值 / 算 IQR 的統計量，只能算「訓練集」的、再套到測試集——又是 data leakage 的常見破口。最穩的是把這些步驟包進 Pipeline（第 20 課）。" },
    ex: { question: "一欄「月收入」有 5% 缺失、且最大值是 9999999（明顯亂填）。你會怎麼處理這兩個問題？", answer: "缺失（5% 不多）：用中位數補（收入偏態、用中位數比平均穩）。9999999：判定為異常 / 亂填、用 IQR 或上限裁切、或設成缺失再補。處理前都先看分佈確認。" },
    xp: 15,
  },
  {
    t: "特徵工程：類別編碼 + 數值縮放",
    s: "模型只吃數字。類別文字要編碼成 0/1（one-hot）、不同範圍的數值要縮放到同尺度，否則大數字會蓋過小數字、模型學偏。",
    a: "幫模型把線索整理好：文字標籤翻成它看得懂的數字（編碼）、把單位不一的數字換算成同一把尺（縮放）。",
    c: [
      "## 模型只能吃數字",
      "- 術語 **feature engineering（特徵工程）** —— 把原始資料加工成模型吃得下的好線索。這兩招最常用。",
      "",
      "## 1. 類別轉數字（encoding、編碼）",
      "- 術語 **one-hot encoding（獨熱編碼）** —— 把「紅/綠/藍」變成 0/1 欄位、彼此平等沒有大小。",
      "",
      "```text",
      "顏色          紅 綠 藍",
      "紅      →      1  0  0",
      "綠      →      0  1  0",
      "# pandas 一行：pd.get_dummies(df[\"顏色\"])",
      "```",
      "> ⚠️ 別把「台北=1、台中=2、高雄=3」這樣編——模型會誤以為有大小順序。沒順序的類別要用 one-hot。",
      "",
      "## 2. 數值縮放（scaling、縮放 / 正規化）",
      "- 術語 **standardization / normalization（標準化 / 正規化）** —— 把範圍差很大的數字拉到同尺度。",
      "",
      "```text",
      "from sklearn.preprocessing import StandardScaler",
      "scaler = StandardScaler()",
      "X_train_s = scaler.fit_transform(X_train)   # 只 fit 訓練集！",
      "X_test_s  = scaler.transform(X_test)        # 測試集只 transform、不 fit",
      "```",
      "- 為什麼要縮放？像 KNN / SVM / 邏輯迴歸 對尺度敏感——「年收入(0~百萬)」會壓過「年齡(0~100)」。",
      "",
      "> 💡 樹模型（決策樹 / 隨機森林 / XGBoost）不太需要縮放；線性 / KNN / SVM / 神經網路 需要。",
      "",
      "**☕ 用人話講**",
      "",
      "模型只吃數字、特徵工程就是幫它備料。**編碼**：把沒有大小的類別（顏色、城市）用 one-hot 拆成 0/1 欄位、別用 1/2/3 騙它有順序。**縮放**：把範圍差很大的數值拉到同尺度、免得大數字蓋過小數字。記得 scaler 只 fit 訓練集。",
    ],
    tip: { type: "practical", text: "縮放器（scaler）一定「fit_transform 訓練集、transform 測試集」。在切分前對全部資料縮放是最常見的資料外洩——下一步學的 Pipeline 會幫你自動做對。" },
    ex: { question: "你有「城市」「年齡」「年收入」三欄。哪些要 one-hot 編碼、哪些要縮放？", answer: "城市 → one-hot（無大小順序的類別）；年齡、年收入 → 縮放（數值、範圍差很大、StandardScaler 拉到同尺度）。" },
    xp: 15,
  },
  {
    t: "特徵工程進階：造新特徵 + 特徵選擇",
    s: "好的「衍生特徵」常勝過換更強的模型；太多沒用的特徵反而干擾。學會「造」有意義的新特徵、再「選」掉沒貢獻的。",
    a: "破案靠關鍵線索、不是線索越多越好。從現有資料推出關鍵線索（造特徵）、再把無關雜訊丟掉（選特徵）、辦案才快又準。",
    c: [
      "## 造新特徵（feature creation）",
      "從現有欄位「算」出更有意義的線索、常常一個好特徵 > 換一個模型：",
      "",
      "```text",
      "# 身高 + 體重 → BMI",
      "df[\"bmi\"] = df[\"weight\"] / (df[\"height\"]/100)**2",
      "# 日期 → 拆出有用的維度",
      "df[\"weekday\"]    = df[\"date\"].dt.dayofweek      # 星期幾",
      "df[\"is_weekend\"] = df[\"weekday\"] >= 5           # 是不是週末",
      "# 兩欄相除 / 相減做出比率、差值（常很有用）",
      "df[\"price_per_ping\"] = df[\"price\"] / df[\"ping\"]  # 每坪單價",
      "```",
      "- 術語 **domain knowledge（領域知識）** —— 懂這個行業、才造得出好特徵（房產知道「每坪單價」比「總價」更可比）。",
      "",
      "## 特徵選擇（feature selection）",
      "- 術語 **feature selection（特徵選擇）** —— 丟掉沒貢獻、甚至會干擾的特徵。太多無關特徵會拖慢訓練、也容易過擬合。",
      "",
      "```text",
      "from sklearn.feature_selection import SelectKBest, f_classif",
      "selector = SelectKBest(f_classif, k=10)   # 留最相關的 10 個特徵",
      "X_new = selector.fit_transform(X_train, y_train)",
      "```",
      "- 也能用「樹模型的 feature_importances_」看哪些特徵重要、再砍尾巴。",
      "",
      "> 💡 原則：**寧缺勿濫。** 10 個有意義的特徵、常勝過 100 個亂塞的。",
      "",
      "**☕ 用人話講**",
      "",
      "特徵工程進階兩件事：**造特徵**——用領域知識從現有欄位算出更有意義的線索（BMI、每坪單價、是不是週末），一個好特徵常勝過換模型；**選特徵**——把沒貢獻的雜訊丟掉，太多無關特徵會干擾又過擬合。寧缺勿濫。",
    ],
    tip: { type: "pro", text: "面試常問「特徵工程怎麼做」。好答案不是背 API、而是舉一個「用領域知識造出關鍵特徵、讓爛模型也變準」的例子——這比會調十種演算法更打動人。" },
    ex: { question: "預測「電商訂單會不會退貨」，你能從「下單時間、商品價格、顧客歷史訂單數」造出哪些有用的新特徵？", answer: "例：是不是深夜衝動下單（下單時段）、價格是否高於該顧客平均（價格/歷史均價）、是不是新客（歷史訂單數=0）、近期退貨率等。重點是用常識推出「跟退貨有關」的線索。" },
    xp: 15,
  },
  {
    t: "線性迴歸：預測數字的基本盤",
    s: "線性迴歸是最簡單、最好解釋的迴歸模型：找一條直線（y = 權重×特徵 + 截距）來預測數字。它是所有迴歸的起點與 baseline。",
    a: "在散點圖上「畫一條最貼合所有點的直線」。之後給一個新 x、用這條線就能讀出對應的 y。",
    c: [
      "## 它在做什麼",
      "- 術語 **linear regression（線性迴歸）** —— 用一條直線（多特徵就是一個平面）擬合資料、預測連續數字。",
      "",
      "```text",
      "房價 = w1×坪數 + w2×屋齡 + ... + b（截距）",
      "       └ 權重 weight（每個特徵的影響力）  └ bias 截距（基準值）",
      "訓練 = 找出讓「預測 vs 實際」誤差最小的那組 w 和 b",
      "```",
      "",
      "📄 **寫進程式檔**",
      "```text",
      "from sklearn.linear_model import LinearRegression",
      "model = LinearRegression().fit(X_train, y_train)",
      "print(\"權重:\", model.coef_)        # 每個特徵的影響力",
      "print(\"截距:\", model.intercept_)   # 基準值",
      "print(\"R²:\", model.score(X_test, y_test))",
      "```",
      "- 術語 **coef_（係數 / 權重）** —— 正值代表「越大越貴」、負值代表「越大越便宜」、絕對值越大影響越強。**這就是線性模型「好解釋」的地方。**",
      "",
      "## 優缺點",
      "```text",
      "✅ 簡單、快、好解釋（看 coef_ 就懂誰影響大）、適合當 baseline",
      "❌ 只能抓「直線關係」、遇到彎曲的規律就不行（要用樹 / 神經網路）",
      "```",
      "",
      "> 💡 做任何迴歸題、先用線性迴歸建一個 baseline 分數、之後更複雜的模型至少要贏過它。",
      "",
      "**☕ 用人話講**",
      "",
      "線性迴歸就是「畫一條最貼合資料的直線」來預測數字。它的權重（coef_）直接告訴你「每個特徵影響多大、是正是負」、超好解釋、又快——所以是迴歸題的標準 baseline。缺點是只會畫直線、抓不了彎曲的關係。",
    ],
    tip: { type: "practical", text: "線性 / 邏輯迴歸對尺度敏感、記得先縮放特徵；而且它好解釋的「係數」是建立信任的利器——跟非工程師解釋模型時，「每多 1 坪 +80 萬」比「黑盒子說的」有說服力多了。" },
    ex: { question: "線性迴歸 model.coef_ 是 [80, -15]（特徵是 [坪數, 屋齡]）。怎麼解讀？", answer: "其他不變下、每多 1 坪房價 +80 萬、每多 1 年屋齡 -15 萬。坪數正向且影響大、屋齡負向。這種「看係數就懂」正是線性模型的價值。" },
    xp: 15,
  },
  {
    t: "邏輯迴歸：分類入門（別被名字騙）",
    s: "邏輯迴歸名字有「迴歸」、其實是做「分類」的：它輸出一個 0~1 的機率、再用門檻（通常 0.5）判成兩類。是分類題最常用的 baseline。",
    a: "考試及格判定：先算出一個「及格機率」（0~1）、超過 0.5 就判及格、否則不及格。邏輯迴歸就是這個「先算機率、再下判斷」。",
    c: [
      "## 它是分類、不是迴歸",
      "- 術語 **logistic regression（邏輯迴歸）** —— 雖叫迴歸、做的是**分類**。先算一個 0~1 的機率、再用門檻分成兩類（是/否）。",
      "- 術語 **sigmoid** —— 把線性結果壓成 0~1 機率的函式（還記得 Ch28b 會再見到它）。",
      "",
      "```text",
      "線性算分 → sigmoid 壓成機率(0~1) → 機率 ≥ 0.5 判「是」、否則判「否」",
      "例：垃圾信機率 0.92 → 判垃圾信；0.13 → 判正常信",
      "```",
      "",
      "📄 **寫進程式檔**",
      "```text",
      "from sklearn.linear_model import LogisticRegression",
      "clf = LogisticRegression(max_iter=1000).fit(X_train, y_train)",
      "print(clf.predict(X_test))          # 直接給類別（0/1）",
      "print(clf.predict_proba(X_test))    # 給機率（可自己調門檻）",
      "```",
      "- 術語 **predict_proba（預測機率）** —— 拿到 0~1 機率、可自訂門檻（怕漏抓就把門檻調低）。",
      "",
      "## 為什麼常用",
      "```text",
      "✅ 快、好解釋（係數看得出每個特徵推向哪一類）、機率可調門檻、分類 baseline 首選",
      "❌ 一樣只抓線性邊界、複雜邊界要用樹 / 集成 / 神經網路",
      "```",
      "",
      "**☕ 用人話講**",
      "",
      "邏輯迴歸名字有「迴歸」、其實是做**分類**：先用 sigmoid 算出一個 0~1 的機率、再用門檻（預設 0.5）判成兩類。它快、好解釋、還能用 `predict_proba` 拿機率自己調門檻（怕漏抓就調低）——是分類題的標準起手 baseline。",
    ],
    tip: { type: "practical", text: "分類別只用 .predict()。拿 .predict_proba() 的機率、依場景調門檻：詐欺 / 疾病這種「寧可錯殺、不可放過」的，把判正例的門檻調低（提高 recall）。這比死守 0.5 實用得多。" },
    ex: { question: "邏輯迴歸做癌症篩檢、預設門檻 0.5 漏掉太多病人。你會怎麼調？副作用是什麼？", answer: "把「判陽性」的機率門檻調低（如 0.3）→ 抓出更多疑似（提高 recall、少漏抓）。副作用：更多健康的人被判陽性（precision 下降、誤報變多、需要複檢）。這是 precision/recall 的取捨。" },
    xp: 15,
  },
  {
    t: "決策樹：會問是非題的模型",
    s: "決策樹用一連串「是非題」把資料一層層切開、最後給答案。它好懂、好解釋（能畫成流程圖）、不需縮放，但單棵容易過擬合。",
    a: "像 20 問猜謎：是動物嗎？會飛嗎？比貓大嗎？一路問下去縮小範圍、最後猜出答案。每個問題就是樹的一個分叉。",
    c: [
      "## 它怎麼決策",
      "- 術語 **decision tree（決策樹）** —— 從根節點開始、每個節點問一個「是非題」（某特徵 > 某值？）、一路往下分叉、葉子給答案。",
      "",
      "```text",
      "          坪數 > 30 ?",
      "         /          \\",
      "      是              否",
      "   屋齡 < 10 ?       判：便宜",
      "   /      \\",
      " 貴       中等",
      "```",
      "",
      "📄 **寫進程式檔**",
      "```text",
      "from sklearn.tree import DecisionTreeClassifier",
      "clf = DecisionTreeClassifier(max_depth=4).fit(X_train, y_train)  # 限制深度防過擬合",
      "print(clf.feature_importances_)   # 哪些特徵在分類時最關鍵",
      "```",
      "- 術語 **max_depth（最大深度）** —— 限制樹長多深。不限制 → 樹會長到「每片葉子一筆資料」、完美背題 = 過擬合。",
      "",
      "## 優缺點",
      "```text",
      "✅ 超好解釋（直接畫成流程圖）、不用縮放、能處理類別 + 數值",
      "❌ 單棵很容易過擬合（所以實務多用「一片森林」→ 下一課隨機森林）",
      "```",
      "",
      "**☕ 用人話講**",
      "",
      "決策樹用一連串是非題把資料切開、像 20 問猜謎、最後給答案。它最大優點是**好解釋**（畫成流程圖人人看得懂）、又不用縮放。缺點是單棵容易把訓練資料背死（過擬合）——所以要限制深度（max_depth）、或乾脆用下一課的「一片森林一起投票」。",
    ],
    tip: { type: "practical", text: "單棵決策樹幾乎不會是最終模型、但它的 feature_importances_ 和「能畫成流程圖」非常適合『跟人解釋模型在想什麼』。理解它、也是理解隨機森林 / XGBoost 的基礎。" },
    ex: { question: "不限制 max_depth 的決策樹，訓練準確率 100%、測試只有 70%。為什麼？怎麼改？", answer: "過擬合：樹長到每片葉子幾乎一筆資料、把訓練集背死。改法：限制 max_depth、min_samples_leaf（每葉最少樣本），或改用隨機森林 / 集成模型。" },
    xp: 15,
  },
  {
    t: "隨機森林：一片森林一起投票",
    s: "隨機森林種很多棵「各看部分資料 / 特徵」的決策樹、讓它們投票。集眾樹之力、比單棵穩、不易過擬合，是表格資料最常用的萬用模型。",
    a: "問一個人容易偏頗、問一百個各有所長的人再投票、結論穩得多。隨機森林就是「找一片各有觀點的樹來投票」。",
    c: [
      "## 從一棵到一片森林",
      "- 術語 **random forest（隨機森林）** —— 種很多棵決策樹、每棵只看「隨機一部分資料 + 隨機一部分特徵」、最後投票（分類）或平均（迴歸）。",
      "- 術語 **ensemble（集成）** —— 把很多弱模型合起來變強的總稱。隨機森林是其中一種。",
      "",
      "```text",
      "🌲🌲🌲🌲🌲  每棵樹看不同的資料 / 特徵、各自給答案",
      "       ↓",
      "      投票 / 平均  →  最終預測（比任何單棵都穩）",
      "```",
      "",
      "📄 **寫進程式檔**",
      "```text",
      "from sklearn.ensemble import RandomForestClassifier",
      "clf = RandomForestClassifier(n_estimators=200, random_state=42).fit(X_train, y_train)",
      "print(clf.score(X_test, y_test))",
      "print(clf.feature_importances_)   # 一樣能看特徵重要度",
      "```",
      "- 術語 **n_estimators（樹的數量）** —— 種幾棵。越多越穩、但越慢；常見 100~300。",
      "",
      "## 為什麼這麼常用",
      "```text",
      "✅ 表格資料常常開箱即用就很強、不太需要調參、不用縮放、不易過擬合",
      "❌ 比單棵慢、比較像黑盒子（但有 feature_importances_ 可看）",
      "```",
      "",
      "> 💡 拿到表格分類 / 迴歸題、隨機森林是「先試了再說」的安全牌。",
      "",
      "**☕ 用人話講**",
      "",
      "隨機森林 = 種一大片「各看部分資料和特徵」的決策樹、讓它們投票。集眾樹之力、比單棵穩、不易過擬合、又不用縮放、開箱即用就很強——所以是表格資料最常用的萬用模型。樹越多越穩但越慢（n_estimators 常用 100~300）。",
    ],
    tip: { type: "practical", text: "表格資料的實戰起手式：邏輯/線性迴歸當 baseline → 隨機森林（幾乎不用調就變強）→ 還想更強再上 XGBoost。隨機森林的 feature_importances_ 也是很好的「哪些特徵重要」分析工具。" },
    ex: { question: "為什麼「很多棵各看部分資料的樹一起投票」會比「一棵看全部資料的樹」更不容易過擬合？", answer: "單棵看全部容易把訓練資料的雜訊也背下來。讓每棵只看隨機一部分、各自會犯不同的錯、投票時這些隨機錯誤互相抵消、剩下的是共識（真規律）——這就是集成「以多補偏」的威力。" },
    xp: 15,
  },
  {
    t: "KNN 與 SVM：兩個經典模型",
    s: "KNN「看最近的鄰居投票」、超直覺；SVM「找一條最寬的分隔線」、小資料高維表現好。兩者都對尺度敏感、用前記得縮放。",
    a: "KNN = 看你身邊最近的 5 個朋友是哪國人、就猜你也是（物以類聚）。SVM = 在兩群人之間畫一條「離兩邊都最遠」的界線、分得最乾脆。",
    c: [
      "## KNN（k 近鄰）",
      "- 術語 **KNN（k-Nearest Neighbors，k 近鄰）** —— 預測時找「最近的 k 個鄰居」、看它們多數屬於哪類就判哪類。",
      "",
      "```text",
      "from sklearn.neighbors import KNeighborsClassifier",
      "clf = KNeighborsClassifier(n_neighbors=5).fit(X_train, y_train)  # 看最近 5 個鄰居",
      "```",
      "- 術語 **k（鄰居數）** —— 太小（k=1）容易被雜訊影響、太大會糊掉邊界。常從 5 試起。",
      "- ⚠️ 對尺度極敏感（算「距離」）→ **一定要先縮放**。",
      "",
      "## SVM（支援向量機）",
      "- 術語 **SVM（Support Vector Machine，支援向量機）** —— 在兩類之間找一條「margin（間隔）最大」的分隔線、分得最有把握。",
      "- 術語 **kernel（核函數）** —— 用 `kernel='rbf'` 能畫出彎曲邊界、處理非線性。",
      "",
      "```text",
      "from sklearn.svm import SVC",
      "clf = SVC(kernel=\"rbf\", C=1.0).fit(X_train, y_train)",
      "```",
      "",
      "| 模型 | 直覺 | 擅長 | 要縮放 |",
      "|---|---|---|---|",
      "| KNN | 看鄰居投票 | 小資料、直覺示範 | 要 |",
      "| SVM | 畫最寬分隔線 | 小資料、高維（如文字） | 要 |",
      "",
      "> 💡 資料量大時 KNN / SVM 會變慢；大資料表格題、隨機森林 / XGBoost 通常更實用。",
      "",
      "**☕ 用人話講**",
      "",
      "**KNN** 超直覺——預測時看最近的 k 個鄰居投票（物以類聚），但對距離敏感、一定要先縮放。**SVM** 在兩類間畫一條「離兩邊都最遠」的分隔線、小資料高維（像文字分類）表現好、加 kernel 還能畫彎的邊界。兩者資料一大就慢、大資料還是隨機森林 / XGBoost 實用。",
    ],
    tip: { type: "practical", text: "KNN 和 SVM 都算「距離」、不縮放就會被大範圍的特徵主導、結果亂掉。凡是用到距離 / 線性邊界的模型（KNN、SVM、邏輯迴歸、神經網路），縮放都是必做的前處理。" },
    ex: { question: "用 KNN 做分類、忘了縮放、模型幾乎只看「年收入」這一欄、忽略「年齡」。為什麼？", answer: "年收入範圍是 0~百萬、年齡是 0~100。KNN 算歐式距離時、年收入的數值差距遠大於年齡、距離幾乎由它主導、年齡形同被忽略。縮放（StandardScaler）把兩者拉到同尺度就解決了。" },
    xp: 15,
  },
  {
    t: "集成學習：XGBoost / LightGBM",
    s: "集成把很多弱模型疊成強模型。Boosting（XGBoost / LightGBM）一棵接一棵、後面專補前面的錯，是 Kaggle 表格賽的常勝軍。",
    a: "接力修正考卷：第一個人先做、第二個人專挑前一個錯的題加強、第三個再補……一棒接一棒、最後整份考卷又快又準。",
    c: [
      "## 兩種集成思路",
      "- 術語 **ensemble（集成）** —— 把多個模型合起來。兩大流派：",
      "  - **Bagging（並聯投票）** —— 很多模型各做各的再投票（隨機森林就是）。",
      "  - **Boosting（串聯補錯）** —— 一個接一個、後面專門修正前面的錯。",
      "",
      "## XGBoost / LightGBM（Boosting 代表）",
      "- 術語 **XGBoost / LightGBM** —— 高效能的 boosting 套件、表格資料常常拿下最佳成績。",
      "",
      "🖥️ **在終端機輸入**",
      "```text",
      "uv add xgboost",
      "```",
      "📄 **寫進程式檔**",
      "```text",
      "from xgboost import XGBClassifier",
      "clf = XGBClassifier(n_estimators=300, learning_rate=0.1, max_depth=5)",
      "clf.fit(X_train, y_train)",
      "print(clf.score(X_test, y_test))",
      "```",
      "- 術語 **learning_rate（學習率）** —— 每棵樹「修正多少」。小一點更穩但要更多棵。",
      "",
      "## 什麼時候用",
      "```text",
      "✅ 想在表格資料榨出最高分（比賽、競爭性任務）、隨機森林贏不夠多時",
      "❌ 參數較多要調、比隨機森林難上手、解釋性低",
      "```",
      "",
      "> 💡 2026 表格資料的「天花板」幾乎都是 XGBoost / LightGBM；圖像文字才換深度學習。",
      "",
      "**☕ 用人話講**",
      "",
      "集成就是「人多力量大」。隨機森林是 **Bagging**（一群樹並聯投票）；**XGBoost / LightGBM** 是 **Boosting**（一棵接一棵、後面專補前面的錯）、是表格賽的常勝軍。想把表格資料分數榨到最高就用它——代價是參數較多要調、也比較像黑盒子。",
    ],
    tip: { type: "pro", text: "Kaggle 表格類競賽的潛規則：特徵工程 + XGBoost/LightGBM + 交叉驗證調參，就能打贏多數方案。面試問「表格資料你會用什麼」，答「先隨機森林建 baseline、再上 XGBoost 並用交叉驗證調參」很穩。" },
    ex: { question: "Bagging（隨機森林）和 Boosting（XGBoost）最大的差別是什麼？", answer: "Bagging 是「並聯」——很多模型各自獨立、各看部分資料、最後投票（降變異、防過擬合）。Boosting 是「串聯」——一個接一個、每個專門修正前一個的錯誤（降偏差、追求更高準度）、但更容易過擬合、要小心調參。" },
    xp: 15,
  },
  {
    t: "過擬合 vs 欠擬合：模型的兩種病",
    s: "欠擬合 = 學得太隨便（連訓練資料都不準）；過擬合 = 背得太死（訓練準、新資料爛）。看「訓練 vs 測試」差距就能診斷。",
    a: "欠擬合 = 書沒讀、考試當然爛；過擬合 = 把考古題答案背死、換題就完蛋；剛好 = 真的讀懂、什麼題都會。",
    c: [
      "## 三種狀態",
      "| 狀態 | 訓練分數 | 測試分數 | 病因 | 解法 |",
      "|---|---|---|---|---|",
      "| 欠擬合 underfitting | 低 | 低 | 模型太簡單 / 特徵不夠 | 換複雜模型、加特徵 |",
      "| 剛好 | 高 | 高 | —— | 維持 |",
      "| 過擬合 overfitting | 超高 | 低 | 太複雜 / 資料太少 | 簡化、加資料、正規化 |",
      "",
      "- 術語 **underfitting（欠擬合）**、**overfitting（過擬合）**、**regularization（正規化，給模型加限制防背死）**。",
      "",
      "## 怎麼診斷：比訓練 vs 測試",
      "```text",
      "兩個都低           → 欠擬合（模型太弱）",
      "訓練高、測試低、差很多 → 過擬合（背起來了）",
      "兩個都高、差不多   → 剛剛好 👍",
      "```",
      "",
      "## 對症下藥",
      "```text",
      "欠擬合 → 用更強的模型、加更多 / 更好的特徵、減少正規化",
      "過擬合 → 簡化模型、加更多資料、加正規化、減少特徵、用交叉驗證確認",
      "```",
      "",
      "**☕ 用人話講**",
      "",
      "模型有兩種病：**欠擬合**（太弱、訓練測試都爛、要加強）、**過擬合**（背太死、訓練超高但測試爛、要收斂）。診斷超簡單——比「訓練 vs 測試」分數：都低是欠擬合、差很多是過擬合、都高且接近才健康。",
    ],
    tip: { type: "practical", text: "畫「學習曲線」（訓練 / 驗證分數隨資料量或複雜度變化）是診斷神器：兩條線都低且貼近 = 欠擬合；訓練高、驗證低且差距大 = 過擬合。看圖比猜準。" },
    ex: { question: "模型訓練 0.72、測試 0.70。是過擬合還是欠擬合？怎麼辦？", answer: "欠擬合（兩個分數都偏低、且接近、沒有背題）。模型太簡單或特徵不夠。改法：換更強的模型（如從線性換隨機森林）、加更多 / 更好的特徵、減少正規化。" },
    xp: 15,
  },
  {
    t: "交叉驗證：別靠切一次的運氣",
    s: "只切一次 train/test、分數會受「剛好切到哪份」影響。交叉驗證把資料切 k 份輪流當考卷、跑 k 次取平均、評估更可靠。",
    a: "把模擬考分成 5 份輪流當正式考、平均成績才看得出真實力——不靠某一次的手氣。",
    c: [
      "## 為什麼要交叉驗證",
      "- 術語 **k-fold cross-validation（k 折交叉驗證）** —— 把資料切成 k 份、輪流拿 1 份當測試、其餘訓練、跑 k 次取平均。",
      "",
      "```text",
      "5-fold（切 5 份、跑 5 次、每次換一份當考卷）：",
      "  第1次 [考][訓][訓][訓][訓]",
      "  第2次 [訓][考][訓][訓][訓]",
      "  …  平均 5 次分數 = 更可靠的實力評估",
      "```",
      "",
      "```text",
      "from sklearn.model_selection import cross_val_score",
      "scores = cross_val_score(model, X, y, cv=5)   # 切 5 折",
      "print(scores.mean(), scores.std())            # 平均 + 波動",
      "```",
      "- 術語 **cv（折數）**，**std（標準差）** —— 波動大代表模型不穩 / 資料分佈不均。",
      "",
      "## 進階：分層 + 跟 Pipeline 搭",
      "- 術語 **stratified（分層）** —— 分類任務切折時保持各類別比例（不平衡資料尤其重要）。",
      "- 把整個 Pipeline 丟進交叉驗證、每折都乾淨重做前處理、不外洩（第 20 課）。",
      "",
      "> 💡 資料少特別要用交叉驗證——一次切分可能剛好切到好（或壞）的那份、平均才公道。",
      "",
      "**☕ 用人話講**",
      "",
      "只切一次 train/test、分數會被「剛好切到哪份」的運氣影響。**交叉驗證**把資料切 k 份輪流當考卷、跑 k 次取平均、評估更可靠（還能看波動 std 判斷穩不穩）。資料越少越要用、分類記得用分層、最好搭 Pipeline 一起跑避免外洩。",
    ],
    tip: { type: "practical", text: "報告模型表現別只給「一次切分的分數」、給「交叉驗證的平均 ± 標準差」更專業也更誠實。標準差大（各折差很多）是個警訊：資料分佈不均、或模型不穩。" },
    ex: { question: "5-fold 交叉驗證分數是 [0.91, 0.62, 0.88, 0.90, 0.89]，代表什麼？", answer: "有一折特別低（0.62）、其餘都高 → 模型大致 OK、但對某份資料表現差很多（可能那折有特殊樣本 / 分佈不均）。值得去看那折、考慮分層切分。平均(約0.84)比任何單折都可靠。" },
    xp: 15,
  },
  {
    t: "評估指標（迴歸）：MAE / MSE / R²",
    s: "迴歸（預測數字）的好壞看誤差：MAE 平均差幾元（好懂）、MSE 重罰大誤差、R² 看「比亂猜好多少」（越接近 1 越好）。",
    a: "射飛鏢評分：MAE = 平均離靶心幾公分；MSE = 離越遠扣越兇（平方放大）；R² = 跟「閉眼亂射」比、你好多少。",
    c: [
      "## 三個常用指標",
      "- 術語 **MAE（Mean Absolute Error，平均絕對誤差）** —— 預測平均差多少（單位跟答案一樣、最好懂：差 50 萬就是差 50 萬）。",
      "- 術語 **MSE / RMSE（均方誤差 / 均方根誤差）** —— 把誤差平方再平均、**大誤差被放大懲罰**（在意「離譜的錯」時看它）。",
      "- 術語 **R²（決定係數）** —— 模型「解釋了多少變異」、範圍 ≤ 1：1=完美、0=跟「永遠猜平均」一樣爛、負的=比亂猜還差。",
      "",
      "```text",
      "from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score",
      "pred = model.predict(X_test)",
      "print(\"MAE :\", mean_absolute_error(y_test, pred))",
      "print(\"RMSE:\", mean_squared_error(y_test, pred) ** 0.5)",
      "print(\"R²  :\", r2_score(y_test, pred))",
      "```",
      "",
      "## 怎麼選",
      "```text",
      "想要好解釋、單位直覺           → MAE（「平均差 X 元」）",
      "很在意「偶爾錯很大」的情況     → MSE / RMSE（重罰大誤差）",
      "想要一個「跟亂猜比好多少」的分 → R²",
      "```",
      "",
      "> ⚠️ R² 是負的不是 bug、是模型比「永遠猜平均值」還爛——通常代表模型 / 特徵有大問題。",
      "",
      "**☕ 用人話講**",
      "",
      "迴歸看誤差：**MAE** 最好懂（平均差多少、單位跟答案一樣）；**MSE/RMSE** 會重罰離譜的大錯（在意極端錯誤時用）；**R²** 告訴你「比永遠猜平均好多少」（1 完美、0 跟亂猜一樣、負的比亂猜還爛）。報告時 MAE + R² 一起給最清楚。",
    ],
    tip: { type: "practical", text: "跟非技術的人解釋迴歸模型、用 MAE：「平均誤差 50 萬」一聽就懂；R² 0.85 對工程師有意義但對老闆抽象。對內看 R²、對外講 MAE。" },
    ex: { question: "預測房價、模型 A 的 RMSE 比 MAE 大很多；模型 B 兩者接近。這代表什麼？", answer: "RMSE 遠大於 MAE → 模型 A 有「少數錯得非常離譜」的預測（大誤差被平方放大、拉高 RMSE）。模型 B 誤差比較平均、沒有極端大錯。若很在意「偶爾大爆走」、B 更可靠。" },
    xp: 20,
  },
  {
    t: "評估指標（分類）：accuracy 為什麼會騙人",
    s: "光看 accuracy 會被騙。分類要看混淆矩陣、再依場景選 precision（少冤枉）或 recall（少漏抓）；不平衡資料看 F1 / AUC。",
    a: "醫院驗病：99% 的人沒病、全猜「沒病」也有 99% 準、卻漏掉每個病人。所以要分開看「抓到的有多準」和「該抓的有沒有漏」。",
    c: [
      "## accuracy 為什麼會騙人",
      "- 術語 **accuracy（準確率）** —— 整體答對比例。陷阱：**不平衡資料**——100 人 1 個有病、全猜沒病 accuracy=99% 卻毫無用處。",
      "",
      "## 混淆矩陣（confusion matrix）",
      "```text",
      "                實際有病     實際沒病",
      "  預測有病        TP（抓對）   FP（冤枉）",
      "  預測沒病        FN（漏抓）   TN（放對）",
      "```",
      "",
      "## 四個關鍵指標",
      "- 術語 **precision（精確率）** = TP/(TP+FP)：抓到的有多少是真的（**怕冤枉看它**）。",
      "- 術語 **recall（召回率）** = TP/(TP+FN)：真的有多少被抓到（**怕漏抓看它**）。",
      "- 術語 **F1** = precision 與 recall 的調和平均（兩者兼顧）。",
      "- 術語 **AUC** = 模型排序好壞的總能力（0.5 亂猜、1.0 完美）、看整體不挑門檻。",
      "",
      "```text",
      "from sklearn.metrics import classification_report, roc_auc_score",
      "print(classification_report(y_test, model.predict(X_test)))  # 一次給 P/R/F1",
      "```",
      "",
      "## 怎麼選：看代價",
      "```text",
      "垃圾信過濾 → 重 precision（別誤刪正常信）",
      "癌症篩檢   → 重 recall（別漏掉病人、寧可多驗）",
      "不平衡資料 → 看 F1 / AUC、別信 accuracy",
      "```",
      "",
      "**☕ 用人話講**",
      "",
      "別只看 accuracy——不平衡資料上它會給假高分。看**混淆矩陣**、再依場景挑：**precision**（怕冤枉）、**recall**（怕漏抓）、**F1**（兼顧）、**AUC**（整體排序能力）。口訣：怕冤枉看 precision、怕漏抓看 recall。",
    ],
    tip: { type: "practical", text: "拿到分類結果先問「資料平不平衡？」平衡 → accuracy 還能看；不平衡（詐欺 / 疾病 / 流失）→ 直接看混淆矩陣 + precision/recall/F1、別被 accuracy 騙。" },
    ex: { question: "詐欺偵測：1000 筆裡 10 筆詐欺。模型抓出 8 筆（全對）、漏 2 筆、沒誤判。precision 和 recall 各多少？", answer: "precision = 8/8 = 1.0；recall = 8/10 = 0.8。詐欺偵測通常更在意 recall（別漏抓）、所以 0.8 是主要改進方向。" },
    xp: 20,
  },
  {
    t: "超參數調校：GridSearch / RandomizedSearch",
    s: "模型有些「旋鈕」（樹幾棵、學習率多少）要人設、叫超參數。GridSearch 全部組合試一遍、RandomizedSearch 隨機抽、搭交叉驗證找最佳組合。",
    a: "調收音機：頻率、音量、等化器幾個旋鈕。GridSearch = 每個刻度都轉一遍找最清楚；RandomizedSearch = 隨機轉幾個位置、省時間又常找到夠好的。",
    c: [
      "## 參數 vs 超參數",
      "- 術語 **parameter（參數）** —— 模型「訓練時自己學」的（線性迴歸的權重）。",
      "- 術語 **hyperparameter（超參數）** —— 「你要先設定」的旋鈕（樹幾棵 n_estimators、學習率 learning_rate、max_depth…）。調得好不好、差很多。",
      "",
      "## GridSearchCV：全組合試 + 交叉驗證",
      "```text",
      "from sklearn.model_selection import GridSearchCV",
      "from sklearn.ensemble import RandomForestClassifier",
      "",
      "grid = {\"n_estimators\": [100, 300], \"max_depth\": [4, 8, None]}",
      "search = GridSearchCV(RandomForestClassifier(), grid, cv=5)  # 每組合都做 5 折 CV",
      "search.fit(X_train, y_train)",
      "print(search.best_params_)     # 最佳超參數組合",
      "print(search.best_score_)      # 對應的交叉驗證分數",
      "```",
      "- 術語 **GridSearchCV** —— 把所有組合 × 交叉驗證跑一遍、挑最好的。組合多會很慢。",
      "",
      "## RandomizedSearchCV：隨機抽、省時間",
      "- 術語 **RandomizedSearchCV** —— 不全試、隨機抽 N 組、通常用少很多時間找到差不多好的組合。**參數空間大時首選。**",
      "",
      "> ⚠️ 調參一定要配交叉驗證、而且**只在訓練集上調**——拿測試集調參 = 變相偷看答案、分數會虛高。",
      "",
      "**☕ 用人話講**",
      "",
      "模型有些「旋鈕」（超參數：樹幾棵、學習率、深度）要你先設、調得好壞差很多。**GridSearch** 把所有組合配交叉驗證試一遍找最佳（準但慢）；**RandomizedSearch** 隨機抽幾組、省時間又常找到夠好的（參數多時首選）。鐵律：調參只用訓練集 + 交叉驗證、別碰測試集。",
    ],
    tip: { type: "practical", text: "別一開始就 GridSearch 海量組合（會跑到天荒地老）。先 RandomizedSearch 大範圍掃、找到不錯的區間、再用 GridSearch 在小範圍精修——又快又有效。" },
    ex: { question: "為什麼調超參數時不能用「測試集」來挑最好的組合？", answer: "因為那等於拿測試集的答案來反覆優化模型、測試集就「被偷看」了、不再是公正的考卷、最終分數會虛高、上線現形。正確做法：用訓練集內的交叉驗證調參、測試集只在最後驗收一次。" },
    xp: 15,
  },
  {
    t: "Pipeline：把前處理 + 模型串成一條",
    s: "用 sklearn Pipeline 把「前處理 + 模型」串成一條、一起 fit/predict。好處：程式乾淨、而且自動避免「測試集資訊外洩」這個致命坑。",
    a: "一條自動化生產線：原料進（原始資料）→ 自動清洗、縮放、組裝（前處理）→ 出成品（預測）。不用手動搬料、也不會搞錯順序。",
    c: [
      "## 為什麼要 Pipeline",
      "手動「先縮放、再切分」很容易讓測試集資訊偷漏進訓練（**data leakage**）。Pipeline 幫你把「前處理只 fit 訓練集」自動做對。",
      "- 術語 **Pipeline（管線）** —— 把多個步驟（前處理 → 模型）串成一個物件、一起 fit、一起 predict。",
      "",
      "```text",
      "from sklearn.pipeline import Pipeline",
      "from sklearn.preprocessing import StandardScaler",
      "from sklearn.linear_model import LogisticRegression",
      "",
      "pipe = Pipeline([",
      "    (\"scaler\", StandardScaler()),     # 第一步：縮放",
      "    (\"model\", LogisticRegression()),  # 第二步：模型",
      "])",
      "pipe.fit(X_train, y_train)            # 縮放只 fit 訓練集、再自動往後套",
      "print(pipe.score(X_test, y_test))     # predict 時自動用同一套縮放",
      "```",
      "",
      "**這段在做什麼？** fit 時前處理只學「訓練集」統計、predict 時自動把同一套套到測試集——**不會外洩**。",
      "",
      "## 跟交叉驗證是絕配",
      "```text",
      "from sklearn.model_selection import cross_val_score",
      "scores = cross_val_score(pipe, X, y, cv=5)   # 每折都乾淨重做前處理、不外洩",
      "```",
      "",
      "> ⚠️ 養成習慣：**任何前處理都包進 Pipeline**、再配交叉驗證、幾乎就不會踩資料外洩。",
      "",
      "**☕ 用人話講**",
      "",
      "Pipeline 把「前處理 → 模型」串成一條生產線、一次 fit、一次 predict。最大好處不只程式乾淨、而是**自動避免資料外洩**：前處理只學訓練集、再自動套測試集。搭配交叉驗證每折都乾淨——這是專業做法、養成習慣少踩超多坑。",
    ],
    tip: { type: "warning", text: "「先對全部資料縮放、再切 train/test」是最隱形又最常見的 bug（外洩、分數假高、上線現形）。解法很簡單：前處理通通包進 Pipeline，順序就永遠對了。" },
    ex: { question: "用 Pipeline 跟「手動先縮放整份再切分」相比、為什麼前者能避免資料外洩？", answer: "手動先縮放整份、scaler 會算進測試集的統計（外洩）。Pipeline fit 時只用訓練集學縮放、predict 才套到測試集——測試集從沒參與「學縮放」、所以乾淨。" },
    xp: 20,
  },
  {
    t: "非監督式：K-means 分群",
    s: "K-means 是最常見的非監督式分群：沒有標籤、自己把資料分成 k 群。常用在客戶分群、找相似、圖片壓縮。",
    a: "把散落的人自動分成幾桌：先隨便擺 k 個桌子中心、每個人去最近的桌子、再把桌子移到該桌人的中間、反覆幾次就穩定。",
    c: [
      "## 從「有答案」跳到「沒答案」",
      "前面都是監督式（有答案）。K-means 是非監督式——沒有答案、自己找出隱藏的群。",
      "- 術語 **K-means（K 均值分群）** —— 你指定分 **k** 群、演算法自動把資料聚成 k 群。",
      "",
      "## 運作直覺（4 步循環）",
      "```text",
      "① 隨機放 k 個中心點",
      "② 每個點歸到最近的中心",
      "③ 中心移到「該群所有點的平均位置」",
      "④ 重複 ②③ 直到中心不再動 → 完成",
      "```",
      "",
      "```text",
      "from sklearn.cluster import KMeans",
      "km = KMeans(n_clusters=3, random_state=42)",
      "labels = km.fit_predict(X)     # 每筆被分到哪群（0/1/2）",
      "```",
      "- 分出來的群**沒有名字**（只有 0/1/2）。「這群是小資族」是**你看完群的特性後自己貼的標籤**。",
      "",
      "## 怎麼決定 k？",
      "- 術語 **elbow method（手肘法）** —— 試不同 k、找「群內誤差下降突然變平」的轉折點（像手肘）。",
      "",
      "> ⚠️ K-means 要先決定群數、且對尺度敏感 → **記得先縮放**（用 Pipeline）。",
      "",
      "**☕ 用人話講**",
      "",
      "K-means 是非監督式分群：你只說「分 k 群」、它用「每個點歸最近中心、中心移到群平均」反覆幾次、自動把相似的聚一起。常用在客戶分群。提醒：要先決定 k（手肘法找）、對尺度敏感要先縮放、分出的群要自己看特性貼標籤。",
    ],
    tip: { type: "practical", text: "客戶分群流程：縮放（Pipeline）→ 手肘法挑 k → KMeans → 逐群看平均特徵、給每群取業務名字（高價值 / 沉睡 / 新客）。縮放別忘、不然「年收入」會壓過「購買次數」。" },
    ex: { question: "K-means 跟分類（classification）最大的差別是什麼？", answer: "分類是監督式（有標籤、學「特徵→正確類別」）；K-means 是非監督式（沒標籤、自己把相似的聚成群、群沒預設意義、要人事後解讀）。一句話：分類照答案學、分群自己找結構。" },
    xp: 15,
  },
  {
    t: "非監督式：PCA 降維",
    s: "特徵太多會又慢又難畫又容易過擬合。PCA 把很多相關的特徵壓成少數幾個「主成分」、保留大部分資訊、常用來視覺化和加速。",
    a: "幫一群人拍「最有代表性的一張側面照」：雖然丟掉一些角度資訊、但用最少的維度抓住最大的差異、看起來還是認得出誰是誰。",
    c: [
      "## 維度太多的麻煩",
      "- 術語 **curse of dimensionality（維度災難）** —— 特徵越多、資料越稀疏、模型越難學、也越難畫圖。",
      "- 術語 **dimensionality reduction（降維）** —— 把很多特徵壓成少數幾個、保留大部分資訊。",
      "",
      "## PCA（主成分分析）",
      "- 術語 **PCA（Principal Component Analysis，主成分分析）** —— 找出資料「變化最大的幾個方向」（主成分）、把資料投影過去、用少數維度抓住大部分資訊。",
      "",
      "```text",
      "from sklearn.decomposition import PCA",
      "pca = PCA(n_components=2)              # 壓成 2 維（方便畫圖）",
      "X_2d = pca.fit_transform(X_scaled)    # 先縮放再 PCA！",
      "print(pca.explained_variance_ratio_)  # 這 2 維保留了多少資訊比例",
      "```",
      "- 術語 **explained_variance_ratio_** —— 每個主成分保留了多少比例的資訊（加起來看保留多少）。",
      "",
      "## 常見用途",
      "```text",
      "📊 視覺化：把高維資料壓成 2D 畫出來、肉眼看分群 / 趨勢",
      "⚡ 加速：特徵太多時先降維、訓練更快、也減少過擬合",
      "```",
      "",
      "> ⚠️ PCA 對尺度敏感、一定要先縮放；而且壓出來的「主成分」是組合、不好直接解釋語意。",
      "",
      "**☕ 用人話講**",
      "",
      "特徵太多會又慢又難畫又容易過擬合。**PCA** 把很多相關特徵壓成少數幾個「主成分」、抓住資料變化最大的方向、保留大部分資訊。最常用在把高維資料壓成 2D 畫圖看分群、或降維加速訓練。記得先縮放、而且主成分不好直接解釋語意。",
    ],
    tip: { type: "practical", text: "拿到「幾十上百個特徵」的資料先別硬塞模型。用 PCA 壓成 2D 畫個散點圖、常常一眼就看出有沒有自然分群、哪裡有結構——比盲調模型更快得到洞察。" },
    ex: { question: "PCA 把 50 個特徵壓成 2 個主成分、explained_variance_ratio_ 是 [0.45, 0.20]。這代表什麼？適合拿來幹嘛？", answer: "前 2 個主成分保留了 45%+20%=65% 的資訊。雖然丟了 35%、但用 2 維就抓住大半變化、很適合「畫成 2D 散點圖做視覺化」、肉眼觀察分群 / 離群。若要餵模型可能要多留幾個成分到保留 ~90%。" },
    xp: 15,
  },
  {
    t: "不平衡資料 + 模型保存與部署",
    s: "詐欺 / 疾病這種「壞的很少」的不平衡資料、要用 class_weight / 過採樣處理、否則模型只會全猜多數類。最後學會把模型存檔、給程式呼叫。",
    a: "班上 1 個作弊 99 個沒作弊——老師偷懶說「全班都沒作弊」也 99% 準、卻抓不到那 1 個。要刻意「多看重那少數」才抓得到。",
    c: [
      "## 不平衡資料（imbalanced data）",
      "- 術語 **imbalanced data（不平衡資料）** —— 某一類遠少於另一類（詐欺、疾病、流失）。模型容易「全猜多數類」拿假高分。",
      "",
      "兩個常用解法：",
      "```text",
      "# 1) 給少數類更高權重（最簡單）",
      "LogisticRegression(class_weight=\"balanced\")",
      "RandomForestClassifier(class_weight=\"balanced\")",
      "",
      "# 2) 過採樣：複製 / 合成少數類（SMOTE）",
      "# uv add imbalanced-learn",
      "from imblearn.over_sampling import SMOTE",
      "X_res, y_res = SMOTE().fit_resample(X_train, y_train)  # 只對訓練集做！",
      "```",
      "- 術語 **class_weight（類別權重）**、**SMOTE（合成少數類過採樣）** —— 都是讓模型「更重視少數類」。",
      "- ⚠️ 過採樣只能對**訓練集**做、且看的是 recall / F1 / AUC、別看 accuracy。",
      "",
      "## 模型保存 + 部署",
      "訓練好的模型要存起來、之後直接載入用、不用每次重訓：",
      "```text",
      "import joblib",
      "joblib.dump(model, \"model.pkl\")          # 存檔",
      "loaded = joblib.load(\"model.pkl\")         # 之後載入",
      "print(loaded.predict([[25]]))             # 直接預測",
      "```",
      "- 術語 **joblib / pickle（序列化）** —— 把訓練好的模型存成檔案。配 Ch26 學的 FastAPI、就能包成一支「給人呼叫的預測 API」。",
      "",
      "**☕ 用人話講**",
      "",
      "不平衡資料（壞的很少）不能只看 accuracy、也不能放著不管——用 `class_weight=\"balanced\"` 或 SMOTE 讓模型更重視少數類（只對訓練集做、看 recall/F1/AUC）。模型訓練好用 `joblib` 存檔、之後載入直接預測；想上線就配 FastAPI 包成預測 API。",
    ],
    tip: { type: "pro", text: "面試陷阱題：「accuracy 99% 的詐欺模型好不好？」——標準錯答是說好。正解：先問正負類比例，不平衡時 99% 可能是全猜多數類、要看 recall / F1 / AUC，並用 class_weight / SMOTE 處理。" },
    ex: { question: "100 萬筆交易、只有 0.2% 是詐欺。你直接訓練、模型 accuracy 99.8%。為什麼這個分數沒意義？兩個改進方向？", answer: "因為全猜「非詐欺」就有 99.8% accuracy、模型可能根本沒抓到任何詐欺。改進：(1) 評估改看 recall / F1 / AUC（看有沒有抓到詐欺）(2) 用 class_weight=balanced 或 SMOTE 讓模型重視少數的詐欺類。" },
    xp: 20,
  },
  {
    t: "模型解釋性：讓模型說人話",
    s: "高準度不夠、還要能解釋「為什麼這樣判」。線性看係數、樹看 feature_importances_、進階用 SHAP——可解釋性是上線、過審、被信任的關鍵。",
    a: "醫生不能只說「電腦判你有病」、得說「因為這三項指標超標」。模型也一樣、要能交代理由、人才敢用。",
    c: [
      "## 為什麼要能解釋",
      "- 術語 **interpretability / explainability（可解釋性）** —— 能說清楚「模型為什麼做這個預測」。金融、醫療、法規場景常「強制要求」、不能用黑盒子下重要決定。",
      "",
      "## 三層工具（由簡到進階）",
      "- **線性 / 邏輯迴歸 → 看 coef_（係數）**：每個特徵推向哪一邊、影響多大。",
      "- **樹 / 隨機森林 / XGBoost → 看 feature_importances_**：哪些特徵最常被拿來分。",
      "- **進階 → SHAP**：對「單一筆預測」拆解每個特徵貢獻多少（正 / 負）、最細緻。",
      "",
      "```text",
      "# uv add shap",
      "import shap",
      "explainer = shap.TreeExplainer(model)",
      "shap_values = explainer.shap_values(X_test)",
      "shap.summary_plot(shap_values, X_test)   # 哪些特徵、把預測往哪推",
      "```",
      "- 術語 **SHAP** —— 用賽局理論「公平分配」每個特徵對這次預測的功勞、是 2026 最常用的解釋工具。",
      "",
      "## 解釋 vs 準度的取捨",
      "```text",
      "線性 / 邏輯迴歸：好解釋、但可能不夠準",
      "XGBoost / 隨機森林：準、但偏黑盒（靠 feature_importances_ / SHAP 補解釋）",
      "→ 看場景權衡：醫療金融重解釋、競賽重準度",
      "```",
      "",
      "> 💡 能把模型「翻譯成人話」（這個客戶被判高風險、是因為近 3 個月逾期 2 次）、比單純高分更有價值、也更容易讓主管 / 客戶買單。",
      "",
      "**☕ 用人話講**",
      "",
      "模型準還不夠、要能解釋「為什麼這樣判」。**線性看係數、樹看 feature_importances_、進階用 SHAP**（把單筆預測拆成每個特徵的貢獻）。好解釋的模型（線性）和高準度的模型（XGBoost）常有取捨——醫療金融重解釋、競賽重準度。能把模型講成人話、是被信任和上線的關鍵。",
    ],
    tip: { type: "pro", text: "面試常問「你的模型怎麼解釋給業務 / 主管聽」。答得出「線性看係數、樹看重要度、單筆用 SHAP 拆貢獻」、再舉一個翻成白話的例子，遠勝只會報 accuracy。可解釋性也是模型能不能過法遵、敢不敢上線的關鍵。" },
    ex: { question: "銀行用 XGBoost 判貸款、被法規要求「必須能解釋每筆拒貸原因」。XGBoost 是黑盒、你怎麼辦？", answer: "用 SHAP 對「每一筆預測」拆解各特徵的貢獻、產生「這筆被拒主要因為 X、Y、Z」的解釋；或同時訓練一個好解釋的邏輯迴歸當對照。重點是：高準度模型 + SHAP 解釋層、兼顧準度與可解釋、滿足法規。" },
    xp: 15,
  },
  {
    t: "ML 學習路徑 + 心法（收尾）",
    s: "你已走完 ML 入門 25 課。三句心法帶走：資料 > 模型、先 baseline 再優化、永遠用沒看過的資料評估。接著往深度學習（Ch28b）或實戰專案前進。",
    a: "這章像學會騎腳踏車的「平衡感」——之後不管換什麼車（演算法）、原理都通。剩下的就是多騎（多做專案）。",
    c: [
      "## 你已經掌握的（整張 ML 入門圖）",
      "```text",
      "基礎：ML 是什麼 / 三類型 / 完整流程 / sklearn / train-test split",
      "資料：清理 / 編碼縮放 / 造特徵選特徵",
      "模型：線性 / 邏輯迴歸 / 決策樹 / 隨機森林 / KNN / SVM / XGBoost",
      "評估：過擬合欠擬合 / 交叉驗證 / 迴歸指標 / 分類指標 / 調參",
      "進階：Pipeline / K-means / PCA / 不平衡資料 / 模型保存部署",
      "```",
      "",
      "## 帶走的三句心法",
      "1. **資料 > 模型** —— 把時間花在資料和特徵、比一直換模型值。",
      "2. **先 baseline、再優化** —— 先用簡單模型跑通拿分、有參照點再進步。",
      "3. **永遠用「沒看過的資料」評估** —— 並依場景選對指標、不然分數是假的。",
      "",
      "## 接下來往哪走",
      "```text",
      "想做圖像 / 文字 / 語音 / 生成式  →  Ch28b 深度學習（下一章）",
      "想練表格實戰                     →  Kaggle 入門賽（Titanic / House Prices）",
      "工具深化                         →  pandas（Ch27）+ sklearn 官方教學",
      "```",
      "",
      "> 💡 學 ML 最快的方法：挑一個你**真的好奇**的資料（消費紀錄、遊戲數據…）、從頭到尾跑一次完整流程。實作 1 次 > 看 10 次。",
      "",
      "**☕ 用人話講**",
      "",
      "走完這 25 課、你有了機器學習的「平衡感」：會跑完整流程、會處理資料、會選模型、會正確評估。帶走三句話——**資料比模型重要、先 baseline 再優化、永遠用沒看過的資料考**——你就不會走歪。接下來想碰圖像文字就進 Ch28b、想練功就上 Kaggle 動手做。",
    ],
    tip: { type: "trend", text: "2026 求職現實：會「跑完整 ML 流程 + 講清楚怎麼評估、為什麼選這模型」的人，比會背一堆演算法名詞的人值錢。面試最愛問「你怎麼知道模型沒過擬合」「accuracy 高為什麼還不能上線」。" },
    ex: { question: "用你自己的話、寫出做任何 ML 專案都該遵守的三條原則。", answer: "(1) 資料 > 模型——先把資料弄乾淨、特徵做好。(2) 先 baseline 再優化——先用簡單模型拿到參照分數。(3) 永遠用沒看過的資料（測試集 / 交叉驗證）評估、並依場景選對指標。" },
    xp: 20,
  },
];

// 圖片佈線（G-4）：lesson 序號(1-based) → [檔名slug, 圖說]。圖放 public/lesson-img/ch28a/，
// 只寫 dark 路徑、light 由 LessonImage 自動推導。圖未生成前會顯示破圖、生好放進去即生效。
const IMG = {
  1: ["ch28a.01_ai_ml_dl", "AI / ML / DL 三層關係"],
  2: ["ch28a.02_ml_3types", "機器學習三大類型：監督 / 非監督 / 強化"],
  3: ["ch28a.03_ml_workflow", "機器學習完整流程七步"],
  4: ["ch28a.04_sklearn_api", "scikit-learn：fit / predict 統一介面"],
  5: ["ch28a.05_train_test_split", "訓練集 / 測試集切分"],
  7: ["ch28a.06_feature_engineering", "特徵工程：編碼 + 縮放"],
  9: ["ch28a.09_algorithms_map", "常見演算法地圖"],
  15: ["ch28a.07_overfit_underfit", "過擬合 vs 欠擬合"],
  16: ["ch28a.10_bias_variance", "偏差–方差權衡"],
  18: ["ch28a.08_metrics_confusion", "混淆矩陣 + 分類評估指標"],
  20: ["ch28a.11_pipeline_preprocess", "Pipeline：前處理 + 模型防外洩"],
  21: ["ch28a.12_clustering_kmeans", "K-means 分群"],
};

const lessons = L.map((x, i) => ({
  id: `77.${i + 1}`,
  number: `LESSON 28a.${i + 1}`,
  title: x.t,
  oneLineSummary: x.s,
  analogy: x.a,
  content: (IMG[i + 1] ? `![${IMG[i + 1][1]}](/lesson-img/ch28a/${IMG[i + 1][0]}_dark.png)\n\n` : "") + J(x.c),
  tip: x.tip,
  exercise: { question: x.ex.question, answer: x.ex.answer },
  xp: x.xp,
}));

const chapter = {
  id: 77,
  slug: "machine-learning",
  stage: 4,
  sortIndex: 28.5,
  title: "機器學習入門",
  subtitle: "從零基礎搞懂 ML、25 課走完資料到模型到評估",
  icon: "brain",
  description: "把 Ch26 的 ML 段獨立擴充成完整一章（25 課）。預設你完全沒碰過機器學習：從「ML 是什麼」「三大類型」「完整流程」、資料清理與特徵工程、七種常用演算法（線性/邏輯迴歸、決策樹、隨機森林、KNN、SVM、XGBoost）、過擬合與交叉驗證、迴歸與分類評估指標、超參數調校、Pipeline、K-means、PCA、不平衡資料、到模型保存部署。每個英文術語都附中文白話、程式碼用 📄/🖥️/💬 標清楚「打哪裡」。",
  status: "published",
  difficulty: "beginner",
  prerequisites: [26],
  estimatedHours: 8,
  outcomes: [
    "講得出 AI / ML / DL 差別、三大類型、完整 ML 流程",
    "會用 scikit-learn 跑 fit / predict、做 train/test split",
    "會資料清理 + 特徵工程（編碼 / 縮放 / 造特徵 / 選特徵）",
    "認得七種常用演算法、知道表格資料先試隨機森林 / XGBoost",
    "懂過擬合 / 交叉驗證、會挑迴歸與分類的評估指標、會調超參數",
    "會用 Pipeline 防外洩、做 K-means / PCA、處理不平衡資料、存檔部署",
  ],
  summary: [
    "ML = 不寫死規則、用資料教電腦自己歸納出規則（學成「模型」）。",
    "三大類型：監督（有答案）、非監督（找結構）、強化（試錯獎勵）；入門先學監督。",
    "流程最花時間的是資料清理 + 特徵工程（約 7~8 成）。",
    "表格資料：邏輯/線性迴歸當 baseline、再上隨機森林 / XGBoost。",
    "永遠用沒看過的資料評估、依場景選指標、用 Pipeline 防資料外洩。",
  ],
  faq: [
    { q: "完全沒數學底子能學機器學習嗎？", a: "入門完全可以。這一章不要求微積分或線性代數——先用 scikit-learn 把流程跑通、建立直覺。想深入「為什麼會動」再補數學也不遲。" },
    { q: "機器學習和 ChatGPT 那種 AI 是同一回事嗎？", a: "ChatGPT 屬於深度學習（DL）、是 ML 裡更進階的一支、下一章 Ch28b 專講。先把這章的 ML 基礎打穩、再進 DL 會輕鬆很多。" },
  ],
};

chapter.lessons = lessons;
fs.writeFileSync(OUT, JSON.stringify(chapter, null, 2) + "\n", "utf8");
console.log("✅ 寫出", OUT, "— lessons:", lessons.length);
